<!-- GENERATED by `gov.py render` from factory.yaml + profile — do not edit by hand -->
# Analyze report — SEC v1 — scope `gate:pass-2`

Generated 2026-09-11T15:54:18+00:00 · contracts C1, C2, C3, C4, C5, C6, C7, C8, C9, C12 · **9 critical · 31 major · 1 minor** · blocking ['CRITICAL', 'MAJOR'] · verdict: BLOCKED

| Severity | Clause | Check | Artifact | Line | Finding |
|---|---|---|---|---|---|
| CRITICAL | C5.12 | data-source | — | 680 | `RULE-SEC-001` declares no `Data source` — the data its check reads has no stated origin |
| CRITICAL | C5.12 | data-source | — | 688 | `RULE-SEC-002` declares no `Data source` — the data its check reads has no stated origin |
| CRITICAL | C5.12 | data-source | — | 696 | `RULE-SEC-003` declares no `Data source` — the data its check reads has no stated origin |
| CRITICAL | C5.12 | data-source | — | 704 | `RULE-SEC-004` declares no `Data source` — the data its check reads has no stated origin |
| CRITICAL | C5.12 | data-source | — | 712 | `RULE-SEC-005` declares no `Data source` — the data its check reads has no stated origin |
| CRITICAL | C5.12 | data-source | — | 720 | `RULE-SEC-006` declares no `Data source` — the data its check reads has no stated origin |
| CRITICAL | C5.12 | data-source | — | 728 | `RULE-SEC-007` declares no `Data source` — the data its check reads has no stated origin |
| CRITICAL | C7.10 | value-agreement | backend-execution-plan | — | `DBF-SEC-064` is ['grant_at'] in `backend-execution-plan` but ['granted_at'] in `db-script` — one of them is a transcription slip |
| CRITICAL | C7.15 | verdict-agrees | backend-execution-plan | 1269 | `backend-execution-plan` claims 0 findings but `gov.py analyze` produced 30 for it — the artifact the implementer reads contradicts the report beside it. Correct line: `BLOCKED ✗ — 30 findings` |
| MAJOR | C4.4 | traces | — | 56 | `US-SEC-006` traces to 0 `POL` id(s), needs ≥1 |
| MAJOR | C4.4 | traces | — | 101 | `US-SEC-011` traces to 0 `POL` id(s), needs ≥1 |
| MAJOR | C4.4 | traces | — | 110 | `US-SEC-012` traces to 0 `POL` id(s), needs ≥1 |
| MAJOR | C7.11 | code-format | backend-execution-plan | 410 | declares the code format `SEC-<3-digit-sequence>` but `stack.backend.api.error_code_format` is `{MOD}-{http}[-{SLUG}]` — a format string maintained as free text drifts from the values it describes |
| MAJOR | C7.11 | code-format | backend-execution-plan | 411 | `SEC-001` is not an instance of the declared format `{MOD}-{http}[-{SLUG}]` |
| MAJOR | C7.11 | code-format | backend-execution-plan | 1213 | declares the code format `SEC-<3-digit>` but `stack.backend.api.error_code_format` is `{MOD}-{http}[-{SLUG}]` — a format string maintained as free text drifts from the values it describes |
| MAJOR | C7.11 | code-format | backend-execution-plan | 1267 | declares the code format `SEC-<3-digit>` but `stack.backend.api.error_code_format` is `{MOD}-{http}[-{SLUG}]` — a format string maintained as free text drifts from the values it describes |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1107 | `Request DTO` names `SignupRequestDto`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1108 | `Request DTO` names `ResetRequestDto`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1109 | `Request DTO` names `ResetCompleteDto`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1110 | `Request DTO` names `(query params)`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1110 | `Response DTO` names `Page\<UserResponse\>`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1114 | `Response DTO` names `DeactivateConfirmation`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1115 | `Response DTO` names `ReactivateConfirmation`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1117 | `Request DTO` names `(query params)`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1117 | `Response DTO` names `Page\<RoleResponse\>`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1120 | `Response DTO` names `RevokeConfirmation`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1123 | `Request DTO` names `ModuleRegisterRequest`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1124 | `Request DTO` names `ScreenRegisterRequest`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1125 | `Request DTO` names `ActionRegisterRequest`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1126 | `Request DTO` names `(query params)`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1126 | `Response DTO` names `Page\<RegistryRowResponse\>`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1128 | `Request DTO` names `(query params)`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1128 | `Response DTO` names `Page\<AuditLogEntryResponse\>`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1129 | `Request DTO` names `(query params)`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1129 | `Response DTO` names `text/csv`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1130 | `Request DTO` names `(query params)`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1130 | `Response DTO` names `Page\<ActiveSessionResponse\>`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1131 | `Response DTO` names `TerminateConfirmation`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.16 | forward-refs | backend-execution-plan | 1132 | `Response DTO` names `List\<ModuleMenuResponse\>`, which `api-docs` does not define — either it is `(proposed)`, or the two disagree |
| MAJOR | C7.17 | xref-surface | backend-execution-plan | 509 | `/api/v1/mdl/lookups` consumes `MDL`'s surface in prose but cites no API of `MDL` — a dependency with no id resolves nowhere, and `MDL`'s own checks never see it |
| MINOR | C7.11 | code-format | backend-execution-plan | — | does not state the declared code format `{MOD}-{http}[-{SLUG}]` (`stack.backend.api.error_code_format`) |
