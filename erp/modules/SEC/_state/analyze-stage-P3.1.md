<!-- GENERATED by `gov.py render` from factory.yaml + profile — do not edit by hand -->
# Analyze report — SEC v1 — scope `stage:P3.1`

Generated 2026-09-12T13:58:09+00:00 · contracts C7, C12 · **0 critical · 57 major · 0 minor** · blocking ['CRITICAL', 'MAJOR'] · verdict: BLOCKED

| Severity | Clause | Check | Artifact | Line | Finding |
|---|---|---|---|---|---|
| MAJOR | C7.19 | required-writer | db-script | 17 | `DBF-SEC-002` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 18 | `DBF-SEC-003` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 19 | `DBF-SEC-004` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 20 | `DBF-SEC-005` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 21 | `DBF-SEC-006` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 22 | `DBF-SEC-007` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 24 | `DBF-SEC-009` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 34 | `DBF-SEC-015` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 35 | `DBF-SEC-016` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 36 | `DBF-SEC-017` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 39 | `DBF-SEC-020` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 49 | `DBF-SEC-026` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 50 | `DBF-SEC-027` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 51 | `DBF-SEC-028` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 52 | `DBF-SEC-029` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 58 | `DBF-SEC-031` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 59 | `DBF-SEC-032` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 60 | `DBF-SEC-033` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 61 | `DBF-SEC-034` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 71 | `DBF-SEC-040` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 72 | `DBF-SEC-041` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 73 | `DBF-SEC-042` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 74 | `DBF-SEC-043` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 75 | `DBF-SEC-044` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 86 | `DBF-SEC-051` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 87 | `DBF-SEC-052` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 88 | `DBF-SEC-053` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 89 | `DBF-SEC-054` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 90 | `DBF-SEC-055` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 100 | `DBF-SEC-061` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 101 | `DBF-SEC-062` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 102 | `DBF-SEC-063` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 103 | `DBF-SEC-064` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 109 | `DBF-SEC-066` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 110 | `DBF-SEC-067` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 111 | `DBF-SEC-068` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 112 | `DBF-SEC-069` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 118 | `DBF-SEC-071` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 119 | `DBF-SEC-072` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 120 | `DBF-SEC-073` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 121 | `DBF-SEC-074` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 127 | `DBF-SEC-076` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 128 | `DBF-SEC-077` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 129 | `DBF-SEC-078` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 130 | `DBF-SEC-079` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 139 | `DBF-SEC-084` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 141 | `DBF-SEC-086` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 151 | `DBF-SEC-092` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 152 | `DBF-SEC-093` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 153 | `DBF-SEC-094` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 154 | `DBF-SEC-095` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 161 | `DBF-SEC-098` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 162 | `DBF-SEC-099` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 163 | `DBF-SEC-100` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 164 | `DBF-SEC-101` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.19 | required-writer | db-script | 165 | `DBF-SEC-102` is NOT NULL in `db-script` but no `API` block names it on a `Request`/`Orchestration` line — nothing in this module writes it, so every operation that depends on it fails on a fresh deployment. Either an endpoint writes it, or the row states why not (system-generated, derived, seeded, DEFERRED) |
| MAJOR | C7.21 | bootstrap-complete | backend-execution-plan | — | `backend-execution-plan` declares 13 permission(s) but `backend-execution-plan` carries no `BOOTSTRAP DATA` section — the module plans its structure and its behaviour and nothing at all about the data that must exist before either can work |

## Coverage — subjects examined per clause

| Clause | Check | Examined |
|---|---|---|
| C7.16 | forward-refs | 46 |
| C7.18 | count-agrees | 0 ⚠ nothing |
| C7.19 | required-writer | 56 |
| C7.2 | traces | 41 |
| C7.20 | operation-resolves | 0 ⚠ nothing |
| C7.21 | bootstrap-complete | 13 |
| C7.22 | orphans | 38 |
| C7.3 | traces | 27 |
| C7.4 | registry-agree | 65 |
| C7.5 | registry-agree | 0 ⚠ nothing |
| C7.5b | registry-agree | 0 ⚠ nothing |
| C7.6 | orphans | 33 |

**4 clause(s) examined nothing**: C7.18, C7.20, C7.5, C7.5b. A clause with no subject is not by itself a defect — a ROOT module really does own no cross-module id — but it enforced nothing on this run, so its verdict is a statement about an empty set. Confirm each is empty by nature and not because the check failed to find its subject.
