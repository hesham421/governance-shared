P3.1 is written, verified and committed (`c3b36a1`).

**Files**
- `erp/modules/NOTE/P3_1/backend-execution-plan-note.md` — extraction working set, index, DB alignment manifest, QRC (6), error catalog (11 rows), the eight profile phases in order, ALIGN, coverage tables
- `erp/modules/NOTE/P3_1/registry-exec-be-note.md`
- `erp/decisions/NOTE/ADR-NOTE-005.md`, `ADR-NOTE-006.md` (both ACCEPTED, non-breaking — run continued)
- `erp/modules/NOTE/_state/briefs/P3.1.response1.md` (the operator response note)

**Shape** — 5 API (`API-NOTE-001..005`, exactly SRS §B5's operations), 6 QR, 0 XM, 1 entity, 1 composite screen `NOTE_NOTES`, 4 permissions. No SUB: API count 5 < 8, entity count 1, XM count 0; `INT-C`/`INT-R` are present and empty as the module registry demands.

**Verification** — `gov.py analyze --scope stage:P3.1` → 0 critical / 0 major / 0 minor; the orchestrator stamped `RESULT: PASSED ✓ — 0 findings`. `gov.py split --track backend --module NOTE --version 1 --dry-run` → 9 files planned, 0 findings, exit 0. Full-module analyze still shows P3.2/api-verify artifacts missing — later stages, not this one.

**Two ambiguities self-resolved:** the five errors with no business rule behind them carry `PLATFORM-STD` rather than a `RULE-*` this stage would have to invent (ADR-NOTE-005); RULE-NOTE-004 gets no catalog row, because AC-NOTE-003 says a client-supplied owner is *ignored*, not rejected, so the row would be raisable by no code path (ADR-NOTE-006).

**Two things worth your attention, both factory-level rather than module-level:**

1. `C7.23` (every operation an SRS screen names is built) **examined nothing**. The clause splits the screen's `Operations` line on `,` `;` `/`; SRS SCR-REQ-NOTE-001 separates that list with `·`, so it parsed no operation and verified nothing. The plan records this explicitly — with the manual resolution endpoint by endpoint — rather than marking the row passed. Any module whose SRS engine emits `·` loses that check silently; the fix belongs in the clause's tokenisation or in the SRS engine's separator, not here.

2. A line that opens with an ID followed by ` (` parses as an ID *definition* in `idmodel`, so a prose-wrapped `Orchestration:` line beginning `DBF-NOTE-001 (…)` truncates the enclosing API record and hides its `required-writer` and `orphans (QR)` evidence. My first draft hit exactly that — four phantom findings. The plan now writes those lines unwrapped, but the trap will catch any engine that wraps at ~90 columns.
e invisible. The plan therefore writes those lines unwrapped. That is a real trap for every engine that wraps prose at 90 columns, and the first draft of this plan fell into it.
