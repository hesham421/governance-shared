Round 2 — written to `erp/modules/NOTE/P0_5/prd-note.md` (as operator, not a file block).

```
══════════════════════════════════════════════════════════════════
PRD EXTRACTION REPORT — NOTE — 2026-09-17  (round 2, final)
══════════════════════════════════════════════════════════════════
STORIES DRAFTED   (7 — unchanged set, no ID re-numbered)
  + US-NOTE-001 … US-NOTE-007   as round 1, with the amendments below
STORIES SKIPPED   unchanged from round 1
QUESTIONS RAISED → RESOLVED
  ? priority scheme        → B, amended: US-NOTE-007 raised to HIGH
  ? deactivated-note access→ B accepted: US-NOTE-005 stands alone
  ? role vocabulary        → B accepted verbatim
  ? SEC self-registration  → B accepted verbatim
POLICIES WITHOUT A STORY (must be empty)
  — (empty)
══════════════════════════════════════════════════════════════════
```

**PROPOSAL 1 — amended.** B is right in shape, wrong on one row: US-NOTE-007 at MEDIUM is a priority inversion. Five HIGH stories (001–004, 006) each deliver nothing until the module and its one screen exist in SEC's registries — G3 turns the whole module into a screen nobody can reach. An enabler every HIGH story depends on cannot rank below them. **US-NOTE-007 → HIGH.** US-NOTE-005 stays MEDIUM, and for a sharper reason than "enabling": it is a second face of the search operation, not a fifth operation. Success metric `—` everywhere: accepted, nothing states one.

**PROPOSAL 2 — accepted (B), and I close your open point: US-NOTE-005 stands alone.** Two reasons beyond yours. Folding it into US-NOTE-004 forces one priority onto two needs of different weight, inflating a secondary need to HIGH. And it hides from P1 that there is an independent mechanism decision to make — a merged story reads as one search behaviour, so "default" quietly becomes the only behaviour. Separate, `POL-NOTE-004`'s word *default* stays load-bearing. Also note the ID grammar forbids re-numbering: folding would have left a gap at 005 or broken the sequence rule.

**PROPOSAL 3 — accepted verbatim.** Two roles plus *the module's integrator*; no invented organizational role.

**PROPOSAL 4 — accepted verbatim.** One story traced to POL-NOTE-006, worded as reachability, never as registration mechanism; `INT-C`/`INT-R` empty-but-present per ADR-FIN-001.

Three editorial amendments I made without a proposal, because they fix contract-facing defects rather than open points:

- **US-NOTE-001's `POL-NOTE-001` trace was unsupported by its own text** — the story never mentioned ownership, so the trace to the owner-visibility policy hung on nothing. Added "and to have it be mine from the moment I write it"; ownership originates at creation, and the Source line now says so.
- **`Status : DRAFT → APPROVED (by the PRD approval gate)`** was the template's field *description* copied literally into each record; it reads as if the stories were already approved. Each record now carries `Status : DRAFT`, with the lifecycle stated once in the scope section.
- **Story sentences were English-only** against the stage's `narrative in ar` policy. Each record now carries a `القصة :` line beside the mandatory `Story :` field (field name untouched, so the record format is intact).
- The `Confirmed by user` column read "ينتظر تأكيد المستخدم", which could be mistaken for an item still open at the gate (C4.5). It now names the gate as the confirming act: approving this file *is* the confirmation.

Nothing material remains open: 7 stories, 6/6 policies covered, 0 deferred, 0 questions.

<!-- CONVERGED -->
