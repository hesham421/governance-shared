{
  "module": "FIN",
  "generated_at": "2026-09-12",
  "current_phase": "ALIGN-BE",
  "current_sub": "ALIGN-BE",
  "api_docs_path": "governance/shared/backend/modules/FIN/api-docs/",
  "phases": [
    {
      "id": "CORE",
      "status": "COMPLETE",
      "subs": [
        {
          "id": "CORE",
          "status": "COMPLETE"
        }
      ]
    },
    {
      "id": "DATA-DOM",
      "status": "COMPLETE",
      "subs": [
        {
          "id": "DATA-DOM-LOOKUP",
          "status": "COMPLETE"
        },
        {
          "id": "DATA-DOM-MASTER",
          "status": "COMPLETE"
        },
        {
          "id": "DATA-DOM-TRANSACTIONAL",
          "status": "COMPLETE"
        }
      ]
    },
    {
      "id": "SVC-API",
      "status": "COMPLETE",
      "subs": [
        {
          "id": "SVC-API-CRUD",
          "status": "COMPLETE"
        },
        {
          "id": "SVC-API-INT",
          "status": "COMPLETE"
        },
        {
          "id": "SVC-API-SEARCH",
          "status": "COMPLETE"
        }
      ]
    },
    {
      "id": "DOC",
      "status": "COMPLETE",
      "subs": [
        {
          "id": "DOC",
          "status": "COMPLETE"
        }
      ]
    },
    {
      "id": "INT-C",
      "status": "COMPLETE",
      "subs": [
        {
          "id": "INT-C",
          "status": "COMPLETE"
        }
      ]
    },
    {
      "id": "INT-R",
      "status": "COMPLETE",
      "subs": [
        {
          "id": "INT-R",
          "status": "COMPLETE"
        }
      ]
    },
    {
      "id": "SEC-BE",
      "status": "COMPLETE",
      "subs": [
        {
          "id": "SEC-BE",
          "status": "COMPLETE"
        }
      ]
    },
    {
      "id": "ALIGN-BE",
      "status": "COMPLETE",
      "subs": [
        {
          "id": "ALIGN-BE",
          "status": "COMPLETE"
        }
      ]
    }
  ],
  "test_phases": [
    {
      "id": "TEST-PLAN-BE",
      "status": "COMPLETE",
      "gated_by_phases": [
        "CORE",
        "DATA-DOM",
        "SVC-API",
        "DOC",
        "INT-C",
        "INT-R",
        "SEC-BE",
        "ALIGN-BE"
      ],
      "header_file": "governance/modules/FIN/test_gen/backend-test-plan-fin.md",
      "subs": [
        {
          "id": "RULE-SCENARIOS",
          "status": "COMPLETE"
        },
        {
          "id": "API-SCENARIOS",
          "status": "COMPLETE"
        }
      ],
      "api_verify_run": "2026-09-12 (re-run after fixes)",
      "coverage": "108/108 in-force TCs covered and passing (TC-FIN-091 RETIRED, excluded); 0 failures",
      "failed_tcs": [],
      "gap_tcs": [],
      "note": "COMPLETE as of 2026-09-12. Every in-force TC is covered and passing across two mechanisms. IMPORTANT for a later reader: /FIN/execute-backend-test STEP 2.1 defines COMPLETE as every TC having a passing *api-verify* counterpart. Eight TCs do not, and cannot: TC-FIN-038/060/061 need a principal holding one FIN permission but not another, which api-verify SKILL.md section 3-I forbids constructing, and TC-FIN-036/056/086/102 need is_retained_earnings_fl, which no endpoint sets, while TC-FIN-044 asserts rows in SEC's registry for which SEC publishes no read endpoint. These eight are covered in-process by JUnit instead, by an explicit recorded human decision taken 2026-09-12 (the user chose the JUnit route over authorising a direct data write or an exception to SKILL.md section 3-I, then directed that the module be closed). The precedent is src/test/java/com/erp/sec/SecCoverageIntegrationTest.java, which exists for the same class of case. No production code was changed to make any test pass. One spec mismatch was found and escalated rather than papered over — see the TC-FIN-056 entry in api_doc_gaps. Full reasoning: governance/project-artifacts/TEST-REPORT-FIN-backend-2026-09-12.md.",
      "mechanisms": {
        "api-verify": "governance/modules/FIN/test-api/test_fin_apis.py — 124 assertions, 0 failures; covers 100 TCs",
        "junit": "src/test/java/com/erp/fin/ — FinSoDCoverageIntegrationTest (3 tests) + FinYearEndCoverageIntegrationTest (7 tests), 10 tests, 0 failures; covers the 8 TCs api-verify structurally cannot reach: TC-FIN-036, 038, 044, 056, 060, 061, 086, 102"
      }
    },
    {
      "id": "INT-XM",
      "status": "COMPLETE",
      "gated_by_phases": [
        "CORE",
        "DATA-DOM",
        "SVC-API",
        "DOC",
        "INT-C",
        "INT-R",
        "SEC-BE",
        "ALIGN-BE"
      ],
      "header_file": "governance/modules/FIN/test_gen/backend-test-plan-fin.md",
      "subs": [],
      "api_verify_run": "2026-09-12",
      "coverage": "1/1 live TC passes (TC-FIN-047, XM-FIN-001 -> MDL); TC-FIN-091 RETIRED",
      "note": "XM coverage 1/1. TC-FIN-047 passes: an MDL lookup failure surfaces as FIN-400-INVALID-LOOKUP, never an MDL code. TC-FIN-091 is RETIRED (XM-FIN-002 -> SEC no longer exists) and is recorded as RETIRED, not as a gap."
    }
  ],
  "blocked": [],
  "deferred_xm": [],
  "api_doc_gaps": [
    {
      "type": "ABSENT",
      "phase": "CORE",
      "sub": "CORE",
      "endpoint": "ENT-FIN-004.docNo (DBF-FIN-035) — API-FIN-019/020/021, 'platform numbering engine'",
      "detail": "CORE.md, srs-fin.md:118/123 and db-script-fin.md:55 all state docNo is generated by a 'platform numbering engine' scoped per fiscalYearId. No such component exists anywhere in this repo: no generator class in com.erp.common or any module, no number-series/sequence-config table in db/migration, and no existing entity with a system-generated business code. No later sub can invent one.",
      "resolution": "HUMAN DECISION RECORDED (final, not to be relitigated). Format: docNo = JV-{fiscalYearCode}-{NNNNNN} — literal prefix 'JV-', then the owning fiscal year's code (FIN_FISCAL_YEAR.code, DBF-FIN-066, VARCHAR(10)), then '-', then a zero-padded 6-digit counter starting at 000001; worked example JV-2026-000123. Worst case length = 3 + 10 + 1 + 6 = 20 chars, comfortably inside doc_no VARCHAR(30). Counter scope and reset: scoped per fiscalYearId, restarting at 000001 for each new fiscal year, which aligns exactly with the existing UQ_FIN_JOURNAL_ENTRY_YEAR_DOCNO (fiscal_year_id, doc_no) constraint — the database therefore backs the uniqueness guarantee. The counter is NOT segmented by journalTypeCode: manual, event-built, recurring, allocation, reversal and the year-end CLOSING/OPENING entries all draw from one counter per fiscal year. Generator ownership: a FIN-local generator living in com.erp.fin, deliberately NOT a shared com.erp.common component — no other module needs document numbering today, so designing a general shared interface now would be speculative; it is promoted into common if and when a second real consumer appears. This is a conscious, recorded deviation from the ecosystem profile's standing guidance that numbering 'comes from the platform numbering engine; never generated in a module' — that guidance presumes an engine which does not exist in this repo, making literal compliance impossible. Immutability (unchanged): docNo is assigned once on create and never changes; excluded from every create and update request DTO, present only in responses. Implementation lands in the SVC-API phase, at journal-entry creation; the entity layer needs nothing beyond an immutable VARCHAR(30) column. Specs updated to match: CORE.md Numbering bullet, P3_1/backend-execution-plan-fin.md (Plan Index business-code row, entity registry row, CORE Numbering, DATA-DOM ENT-FIN-004 business code, SVC-API create orchestration) and packages/backend-execution/_SECTIONS.md.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "CORE",
      "sub": "CORE",
      "endpoint": "Error Catalog row FIN-503 — any lookup-validating API (XM-FIN-001 unreachable)",
      "detail": "The catalog declares a 503 outcome, but com.erp.common.domain.status.Status has no SERVICE_UNAVAILABLE constant (highest is INTERNAL_ERROR -> 500), so LocalizedException cannot express HTTP 503. Adding one would modify shared common infrastructure, which is outside FIN's scope.",
      "resolution": "Resolved — the FIN-503 catalog row is unreachable and is struck; the Error Catalog goes from 32 rows to 31. Why: cross-module consumption in this repo is direct Spring interface injection within a single deployable, not loopback HTTP (see the javadoc on src/main/java/com/erp/mdl/crossmodule/MdlLookupApi.java, and the two delivered consumers NotificationLookupService and FileLookupService, which inject MdlLookupApi and call it in-process). With no network hop, 'MDL unreachable / 503' has no runtime producer. Corroborating: com.erp.common.domain.status.Status has no SERVICE_UNAVAILABLE constant (it tops out at INTERNAL_ERROR -> 500), GlobalExceptionHandler derives the HTTP status solely from Status, and gov-enforce-error-handling forbids a feature module modifying the shared enum — so 503 was unreachable by any sanctioned path regardless. Behaviour FIN adopts instead, following NotificationLookupService's delivered pattern: an invalid lookup code raises FIN-400-INVALID-LOOKUP; any other failure falls through to the existing, reachable FIN-500.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "DATA-DOM",
      "sub": "DATA-DOM-TRANSACTIONAL",
      "endpoint": "RULE-FIN-016 (lock an entry after posting) — every POSTED-entry mutation attempt; ENT-FIN-004.statusCode",
      "detail": "The Error Catalog in packages/backend-execution/_SECTIONS.md registers no code for RULE-FIN-016; its own RULE->code coverage line says '(enforced by omission, no code needed — no UPDATE/DELETE mapping exists on a POSTED row)'. srs-fin.md §A5 nevertheless gives the rule a bilingual message ('القيد المُرحَّل مقفل؛ التصحيح فقط عبر العكس' / 'A posted entry is locked; correction is only through a reversal'), and CORE.md mandates a domain guard for every rule answering 'is this operation allowed?' — so JournalEntryDomain.assertNotPosted() needs a registered code to throw and the catalog supplies none. Inventing one is forbidden by the contract ground-truth rule.",
      "resolution": "RESOLVED — not deferred, and there is no spec gap to clarify. The positive guard JournalEntryDomain.assertNotPosted() has been removed as a latent bug: it would have rejected every legal reversal. FIN's API registry declares no PUT and no DELETE on /journal-entries, so the only code path that ever writes an already-POSTED row is the reversal flow (API-FIN-021), and that write is deliberate — SVC-API-INT.md specifies setting originalEntryId/reversalEntryId on both rows, SRS A7's state machine defines POSTED --(reverse action)--> VOID for the original, and JournalEntry.voidByReversal(...) performs exactly that. The guard's own javadoc told the service to call it 'before any mutation of an existing entry'; wired as instructed it would have thrown on every reversal. RULE-FIN-016's own SRS text carves this out ('reject any edit or delete attempt... correction is only through a reversal'), so reversal is the rule's exception, not its target. Left uncalled instead it would be dead code, which gov-validate-backend-feature and contract rule A.2.9 both flag. RULE-FIN-016 is therefore enforced by omission exactly as the execution plan already states ('enforced by omission, no code needed — no UPDATE/DELETE mapping exists on a POSTED row'): JournalEntryRepository declares no @Modifying query and no derived deleteBy..., which is the enforcement mechanism and stays untouched. Precedent: MDL's RULE-MDL-003 (key immutability) — owned by LookupTypeDomain for traceability, carrying its SRS message, enforced purely structurally, with no guard method and no error code, and independently verified compliant. JournalEntryDomain's class javadoc now documents the rule the same way, including the reversal carve-out. FIN-409-INVALID-TRANSITION stays in FinErrorCodes and in both message bundles as the period soft-close row it actually is; its javadoc no longer claims to cover POSTED-entry mutation. No new error-code row should ever be requested for RULE-FIN-016.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "SVC-API",
      "sub": "SVC-API-CRUD",
      "endpoint": "POST /api/v1/fin/accounts (API-FIN-002) — parentAccountId handling, RULE-FIN-001 create trigger",
      "detail": "API-FIN-002's Orchestration line and its Errors line specify mutually exclusive behaviours for the same input. Orchestration says: 'if parentAccountId given, flip the parent's isLeafFl to false via RULE-FIN-001's inverse effect (a parent gaining its first child can no longer itself be a leaf)'. Errors lists FIN-409-PARENT-NOT-LEAF-ELIGIBLE, and the delivered DATA-DOM-MASTER domain guard AccountDomain.create(code, leaf, parentIsLeaf) throws exactly that code when the chosen parent is still marked leaf, with javadoc stating 'the parent must be demoted first (Account.markAsParent())'. Auto-flipping would make the guard unreachable and FIN-409-PARENT-NOT-LEAF-ELIGIBLE dead in both the code table and both message bundles; rejecting means no flip ever happens.",
      "resolution": "Implemented in favour of the delivered domain guard: AccountService.create resolves the parent, then calls AccountDomain.create(...), which rejects a parent still marked as accepting direct posting with FIN-409-PARENT-NOT-LEAF-ELIGIBLE. No code change was needed for this resolution — the delivered behaviour already matches it (verified against AccountService.create and AccountDomain.create). Grounding (strengthened; the earlier 'DATA-DOM is COMPLETE' argument was the weakest of the available ones and is no longer the basis): (1) RULE-FIN-001's own scope in srs-fin.md §A5 states 'Trigger: on update (isLeafFl)' — the rule is triggered by an attempt to change isLeafFl, and it never authorises the system to flip a parent's flag by itself on a child-create; (2) AC-FIN-002 says 'Then the system rejects the change', which is rejection, not silent mutation; (3) the API-FIN-002 block's own Validations line already says the check targets the parent and expects rejection — 'at create this only matters when the same call sets a parent that itself must remain non-leaf — QR-FIN-006 checks the *parent*, not the new row'; (4) under the auto-flip reading the catalogued code FIN-409-PARENT-NOT-LEAF-ELIGIBLE would have no trigger anywhere in the system — it is catalogued against API-FIN-002 only, so auto-flipping would leave it dead in FinErrorCodes and in both message bundles. The erroneous line is therefore the block's Orchestration sentence ('if parentAccountId given, flip the parent's isLeafFl to false via RULE-FIN-001's inverse effect'), which contradicts the same block's Validations and Errors lines as well as the SRS rule and its AC. Demotion of a parent remains available explicitly through API-FIN-003. No spec file was edited.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "SVC-API",
      "sub": "SVC-API-CRUD",
      "endpoint": "POST /api/v1/fin/journal-entries (API-FIN-019) — 'every failure returned together (REQ-FIN-015)'",
      "detail": "API-FIN-019's Validations line requires RULE-FIN-006/007/008/009 failures to be 'returned together', but the shared error envelope cannot express that: com.erp.common.exception.LocalizedException carries exactly one Status and one errorCode, GlobalExceptionHandler renders it as a single ApiError, and the multi-item FieldErrorItem list is populated only by bean-validation (MethodArgumentNotValidException). The four delivered domain guards (JournalEntryDomain.assertBalanced/assertTargetPeriodOpen, AccountDomain.assertPostable, DimensionValueDomain.assertUsableOnLine) each throw on first violation by construction. Aggregating them would require either modifying shared common infrastructure (forbidden) or reimplementing the rules outside the domain classes (A.5.18 automatic rejection).",
      "resolution": "RESOLVED — implemented, not deferred. The requirement was real and the delivered fail-fast behaviour did violate AC-FIN-015 ('Given a manual entry with two failing validations ... Then the system returns both failures and posts nothing'). A human explicitly approved an additive change to com.erp.common to close it; that approval is the sole basis for touching shared infrastructure and its scope is exactly what is listed here. Shape implemented: (1) new record com.erp.common.exception.ErrorDetail(String errorCode, Object... args) — one failure carrying exactly what a single-code LocalizedException carries, so each entry localizes through the existing MessageSource lookup identically; (2) a second LocalizedException constructor (Status, List<ErrorDetail>) alongside the untouched (Status, String, Object...) one — the first detail supplies the existing top-level errorCode/args, the whole list is kept in a new transient List<ErrorDetail> errors that is List.of() for every single-code throw; (3) GlobalExceptionHandler.handleLocalizedException now also fills the fieldErrors list ApiError already exposed — one FieldErrorItem per failure (field = error code, message = the resolved bundle message) — only when errors is non-empty. Status enum, the Status->HTTP mapping and ApiError's fields are unchanged, so a single-code throw serializes byte-identically to before and all 140 existing LocalizedException call sites across sec/mdl/cu/notif/file/fin compile and behave unchanged. Domain layer: each guard gained a NON-THROWING sibling returning Optional<ErrorDetail> — JournalEntryDomain.checkBalanced / checkTargetPeriodOpen, AccountDomain.checkPostable, DimensionValueDomain.checkUsableOnLine — and the existing assertBalanced / assertTargetPeriodOpen / assertPostable / assertUsableOnLine now delegate to them, so every rule keeps ONE implementation and the throwing API is unchanged for every other caller (event build, recurring, allocation, reversal). JournalEntryService.createManual evaluates every applicable guard through the check* siblings, collects the returned ErrorDetails and throws ONCE with Status.CONFLICT and the full list (all four codes are 409, so the HTTP status is unchanged); the service contains no business condition of its own, so A.5.18 still holds. Not-found preconditions (year, period, account, dimension, dimension value) and XM-FIN-001 lookup validation deliberately stay fail-fast: an unresolvable reference leaves nothing to validate. Atomicity is untouched — the aggregate is still assembled in memory, the aggregate throw happens before JournalEntry.post(...) and before the single repository.save(...), inside the one @Transactional method. Verified: mvn compile and the full mvn test suite (8 tests, incl. CrossModuleBoundaryArchTest) pass on JDK 25.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "SVC-API",
      "sub": "SVC-API-INT",
      "endpoint": "POST /api/v1/fin/journal-entries/from-event (API-FIN-020) — request body shape, and ACCOUNT_DERIVATION_TYPE=MAPPING",
      "detail": "API-FIN-020's Request line is 'the canonical accounting event payload (opaque shape, out-of-scope Event consumer, POL-FIN-020)' — no field list exists anywhere in P1/P2/P3_1, yet the rule engine must read named fields off it: a RuleLine with accountDerivationTypeCode=DIRECT names an event field holding an account code (DBF-FIN-102), and one with amountSourceTypeCode=FIELD names an event amount field (DBF-FIN-104). Separately, accountDerivationTypeCode=MAPPING implies a mapping store that db-script-fin.md declares nowhere: ENT-FIN-010 carries only the type/value pair and there is no mapping table among the 14 FIN tables.",
      "resolution": "HUMAN DECISION APPLIED (final). (a) MAPPING now FAILS LOUDLY instead of being collapsed into DIRECT. srs-fin.md ENT-FIN-010 defines accountDerivationValue as 'constant account code, event field name, or mapping-set key' — one meaning per ACCOUNT_DERIVATION_TYPE — and no mapping store exists in db-script-fin.md's 14 tables, so reading a mapping-set key as an event field name silently resolved a line to the wrong account or to none: the one outcome a ledger must never produce. A new Error Catalog row FIN-422-MAPPING-UNSUPPORTED (PLATFORM-STD, API-FIN-020, HTTP 422, ar: 'يستخدم هذا السطر اشتقاق الحساب عبر جدول المطابقة وهو غير متاح حاليًا' / en: 'This rule line uses mapping-based account derivation, which is not available yet') was added to _SECTIONS.md and the plan's copy, registered as FinErrorCodes.FIN_422_MAPPING_UNSUPPORTED and added to BOTH message bundles. HTTP 422 / Status.BUSINESS_RULE_VIOLATION was chosen because the request is structurally valid (not 400) and nothing is missing on the wire (not 404) and no record blocks it (not 409): a stored configuration cannot be executed, which is exactly the taxonomy's 'invariant violation that is not about a specific referencing record'. 501 was unavailable — Status declares no NOT_IMPLEMENTED and a feature module may not extend the shared enum (the same constraint that struck FIN-503). EventTypeRuleDomain.resolveAccountCode now branches explicitly: CONSTANT returns the value as the account code, MAPPING throws the new code, anything else (DIRECT) reads the named event field — the MAPPING fall-through into the DIRECT branch is gone. CONSTANT and DIRECT keep working unchanged. (b) The request-shape half of this gap is UNCHANGED and still open: EventEntryBuildRequest keeps the narrowest shape that invents nothing ({eventReference, eventTypeCode, docDate, baseAmount, amounts, fields, descriptionAr, descriptionEn}) pending the Event consumer's real contract.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "SVC-API",
      "sub": "SVC-API-INT",
      "endpoint": "POST /api/v1/fin/fiscal-years (API-FIN-023) — generated periods' NAME_AR / NAME_EN and their date spans",
      "detail": "API-FIN-023's Request is {code, startDate, endDate, periodCount} and its Orchestration says 'generate periodCount periods, each statusCode=OPEN'. FIN_FISCAL_PERIOD.NAME_AR and NAME_EN are NOT NULL (DBF-FIN-078/079, db-script-fin.md §3 BLOCK 3, V22__fin_schema.sql) and START_DATE/END_DATE are NOT NULL too (DBF-FIN-080/081), but nothing in the SRS, db-script or execution plan says what a generated period is named or how the year's span is divided.",
      "resolution": "HUMAN DECISION APPLIED (final) — periods are calendar months when there are twelve. The previous even split of DAYS produced 31/31/31/30… blocks whose boundaries drifted off month ends, so entries landed in the wrong period and every period-scoped report was wrong. Rule now governed, not implicit: SVC-API-INT.md's API-FIN-023 block (and the plan's copy) states it — when periodCount = 12 and [startDate, endDate] covers one whole year starting on the first of a month, period N IS the Nth calendar month of the span (1st to last day) and its NAME_AR/NAME_EN are that month's own name in Arabic and English; for any other period count, or a span that is not a whole year, the even day split remains the fallback, with the first (totalDays mod periodCount) blocks one day longer and 'الفترة N' / 'Period N' names. Implemented in FiscalPeriodDomain.generatedPeriod(yearStart, yearEnd, periodNo, periodCount), returning a GeneratedPeriod(startDate, endDate, nameAr, nameEn) record; FiscalYearMapper.toPeriodEntity now only builds the row from it. Month names come from the platform's own locale data — java.time.Month.getDisplayName(TextStyle.FULL, locale) over the JDK's CLDR data, for Locale 'ar' and Locale.ENGLISH — not a hardcoded Arabic string table. The project's MessageSource bundles were NOT used for them: those bundles are the error-code/validation-key register (keys are wire codes) and the codebase exposes no localization mechanism for persisted row content; CLDR is the standard source for calendar names and needs no per-deployment upkeep. Only the fallback labels 'الفترة '/'Period ' remain as literals, since a non-calendar block has no name in any locale data.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "SVC-API",
      "sub": "SVC-API-INT",
      "endpoint": "POST /api/v1/fin/fiscal-years/{id}/year-end-close (API-FIN-027) — Retained Earnings account, next fiscal year, and RULE-FIN-008 on the closing entry",
      "detail": "Three problems in one block. (1) The Orchestration line says 'result accounts -> Retained Earnings' but FIN_ACCOUNT carries no retained-earnings marker (DBF-FIN-001..013) and ACCOUNT_TYPE only reaches EQUITY, which many accounts can share — the account cannot be derived. (2) It says 'build + post the next year's opening entry' but FIN_FISCAL_YEAR declares no successor link (DBF-FIN-065..074); inferring one from adjacent dates would invent a rule. (3) CONTRADICTION: the API's own Validations line requires every period of the year to be HARD_CLOSE before the close runs, while its Repository line routes BOTH generated entries through QR-FIN-029..033 — which includes QR-FIN-031, RULE-FIN-008's 'the entry's period is Open at post time'. Under that reading the closing entry can never post, because no Open period exists in the year by construction.",
      "resolution": "HUMAN DECISION APPLIED (final) — all three parts; the invented YearEndCloseRequest is DELETED and API-FIN-027's request is the path id again, exactly as the plan states. (1) RETAINED EARNINGS gets a real marker. New forward migration V23__fin_account_retained_earnings_flag.sql (V22 untouched) adds FIN_ACCOUNT.is_retained_earnings_fl BOOLEAN NOT NULL DEFAULT FALSE and guarantees AT MOST ONE marked account through the partial unique index UQ_FIN_ACCOUNT_RETAINED_EARNINGS ON FIN_ACCOUNT (is_retained_earnings_fl) WHERE is_retained_earnings_fl — partial, because a plain UNIQUE over the column would also cap the number of FALSE rows at one; Postgres has no partial UNIQUE *constraint*, hence CREATE UNIQUE INDEX. Registered as DBF-FIN-147 in db-script-fin.md's FIN_ACCOUNT matrix, CREATE TABLE, COMMENT and index blocks, in the DB Alignment Manifest in both _SECTIONS.md and P3_1 (counts 146 -> 147), and in P2/registry-db-fin.md. Account gains the mapped field (native BOOLEAN, no converter per A.1.6; the partial index is documented on the field rather than mis-declared as a total @UniqueConstraint), AccountResponse and AccountMapper expose it READ-ONLY, and it is deliberately NOT settable through API-FIN-002/003: SVC-API-CRUD.md fixes those request contracts field by field and neither lists it, and a caller-settable marker would reinstate exactly the unaudited ledger decision this replaces; it is set as data, and a governed endpoint would need a new API id. FiscalYearService.retainedEarningsAccount() derives it via AccountRepository.findFirstByIsRetainedEarningsFlTrueOrderByAccountPkAsc(); none marked raises the existing FIN-404-ACCOUNT (no new code invented), and a second marked account is unreachable behind the index. (2) RULE-FIN-008's exemption is now part of the RULE. srs-fin.md §A5 RULE-FIN-008's Statement was amended (the single sanctioned edit to that file) to exempt the year-end closing and opening entries, noting the exemption applies to no other entry and to no other rule; the wording was propagated to the DATA-DOM ENT-FIN-008 block (DATA-DOM-MASTER.md), DATA-DOM-TRANSACTIONAL.md, SVC-API-INT.md and both the _SECTIONS.md and P3_1 copies (including the QR-FIN-031 registry row). API-FIN-027's Repository line — the genuinely imprecise line — now reads QR-FIN-041, QR-FIN-029, QR-FIN-030, QR-FIN-032, QR-FIN-033 and states that QR-FIN-031 is not applied to either generated entry. In code the parameterised hole is GONE: JournalPostingService.PostingRequest.periodGateApplies is deleted (zero occurrences in src/) and the exemption lives in the domain layer as JournalEntryDomain.checkTargetPeriodOpenForGeneratedEntry(journalTypeCode, statusCode), which decides from what the entry IS (isYearEndGenerated: JOURNAL_TYPE CLOSING/OPENING, constants moved onto JournalEntryDomain and re-exported by JournalPostingService) and otherwise delegates to the unchanged single implementation checkTargetPeriodOpen(statusCode). API-FIN-019's manual path keeps the unconditional form on purpose, so a caller-supplied journalTypeCode can never buy the exemption. RULE-FIN-006/007/009 still apply to both generated entries in full. The closing entry posts into the year's LAST period, as before. (3) SUCCESSOR YEAR — DERIVED DECISION, MADE IN THE ABSENCE OF AN ARTIFACT, OPEN TO BEING OVERRULED: no spec states how the next fiscal year is found and FIN_FISCAL_YEAR declares no successor column (DBF-FIN-065..074), so the only derivation the schema supports was taken — ADJACENCY: the fiscal year whose startDate is the day after this year's endDate (FiscalYearRepository.findByStartDate, FiscalYearService.successorOf). No such year raises the existing FIN-404-YEAR rather than guessing. This is NOT something the specs stated; it is recorded in SVC-API-INT.md's API-FIN-027 Repository line as DERIVED and should be replaced by an explicit successor link or a stated rule if a human decides otherwise.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "SVC-API",
      "sub": "SVC-API-INT",
      "endpoint": "POST /api/v1/fin/recurring-templates/{id}/run (API-FIN-014) — no not-found error code for a RecurringTemplate",
      "detail": "API-FIN-014 loads the template by id (QR-FIN-019) and its Errors line says only 'same as API-FIN-019', whose codes are the four post-time rule codes plus FIN-404-YEAR / FIN-404-PERIOD / FIN-404-ACCOUNT. The Error Catalog in _SECTIONS.md carries no FIN-404-TEMPLATE row (nor any other row for ENT-FIN-011), so an unknown template id has no registered code to raise, and inventing one is forbidden by the contract ground-truth rule.",
      "resolution": "HUMAN DECISION APPLIED (final) — FIN-404-TEMPLATE now exists. The catalog assigns a distinct 404 per entity (ACCOUNT, DIMENSION, RULE, ALLOCATION-RULE, ENTRY, PERIOD, YEAR) and had no row for ENT-FIN-011, so an unknown recurring template id was reported as FIN-404-RULE ('Rule not found'), naming the wrong entity to the client. A new row in the same uniform style — | FIN-404-TEMPLATE | PLATFORM-STD | API-FIN-014 | 404 | unknown recurring template id | القالب المتكرر غير موجود | Recurring template not found | — was added to the Error Catalog in _SECTIONS.md and the plan's copy, registered as FinErrorCodes.FIN_404_TEMPLATE with Status.NOT_FOUND, added to both message bundles, and is now raised by the one orElseThrow in RecurringTemplateService.run. API-FIN-014's Errors line reads 'same as API-FIN-019, plus FIN-404-TEMPLATE'. FIN-404-RULE stays exactly where it belongs, on the event-type rule (RuleLineService).",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "SVC-API",
      "sub": "SVC-API-SEARCH",
      "endpoint": "GET /api/v1/fin/reports/income-statement (API-FIN-031) — the 'period range' request parameter",
      "detail": "API-FIN-031's Request line is 'fiscalYearId(EXACT), period range'. 'period range' names no field anywhere: srs-fin.md REQ-FIN-042 / AC-FIN-042 and its A8 screen table say only 'fiscalYearId, period range', db-script-fin.md defines no range column, and the plan's API registry, QRC (QR-FIN-043) and DB Alignment Manifest carry nothing either. Neither the parameter names nor their type (period ids? period numbers? dates?) are stated, so the shape had to be chosen rather than read. Separately, the block's Errors line names only FIN-500, which leaves an unresolvable range bound with no stated outcome.",
      "resolution": "RESOLVED at the DOC phase — no longer blocked, and no spec clarification is needed. What the spec left unstated was the PARAMETER NAMES, not the semantics: 'period range' has exactly one meaning the delivered artifacts can support, and the implementation took it without inventing anything. Verified at DOC against all three grounds. (1) SCHEMA — the range needs no new column or concept: FIN_FISCAL_PERIOD already carries fiscal_period_pk (DBF-FIN-075) plus start_date (DBF-FIN-080) and end_date (DBF-FIN-081), all NOT NULL, in db-script-fin.md §1/§3 and in V22; a range of periods therefore already IS a range of dates, derivable from rows that exist. (2) SIBLING PRECEDENT — the other reports scope identically. ReportService.trialBalance (API-FIN-029), balanceSheet (API-FIN-030) and incomeStatement (API-FIN-031) all narrow through the SAME shared QR-FIN-043 aggregation, the single private ReportService.balanceRows(periodId, fiscalYearId, fromDate, toDate, accountTypeCodes) call onto JournalLineRepository.aggregateAccountBalances(...); balanceSheet already passes a docDate bound (asOfDate) into that same toDate parameter, so the income statement translating its two period ids into the fromDate/toDate bounds of that identical signature is the established pattern, not a new mechanism. (3) NO INVENTED ERROR CODE — an unresolvable period id raises the ALREADY REGISTERED FIN-404-PERIOD (FinErrorCodes.FIN_404_PERIOD, Status.NOT_FOUND, present in both i18n bundles: messages.properties 'Period not found' / messages_ar.properties 'الفترة غير موجودة') from ReportService.periodOf; no catalog row was added and API-FIN-031's Errors line (FIN-500) is untouched. DELIVERED SHAPE, unchanged by this resolution: two OPTIONAL query parameters fromPeriodId and toPeriodId on GET /api/v1/fin/reports/income-statement, translated to fromPeriodId -> its startDate and toPeriodId -> its endDate; both omitted means the whole fiscal year, which is REQ-FIN-042's own scope; the applied range is echoed back in IncomeStatementResponse.fromPeriodId/toPeriodId/fromDate/toDate so the client sees exactly what was used. The names fromPeriodId/toPeriodId are recorded as a NAMING CONVENTION chosen for consistency with the module's existing periodId/fiscalYearId/asOfDate report parameters — a convention, not an invented contract term; a human may rename them without any semantic consequence.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "INT-C",
      "sub": "INT-C",
      "endpoint": "XM-FIN-001 — keys FISCAL_YEAR_STATUS (DBF-FIN-069), PERIOD_STATE (DBF-FIN-082), JOURNAL_STATUS (DBF-FIN-040)",
      "detail": "INT-C.md states FIN calls MdlLookupApi.readActiveValuesByKey(typeKey) 'for every one of FIN's 13 owned keys, at the point each lookup-backed field is written'. Nine keys have a real call site (ACCOUNT_TYPE, DEBIT_CREDIT, JOURNAL_TYPE, ACCOUNTING_EVENT_TYPE, ACCOUNT_DERIVATION_TYPE, AMOUNT_SOURCE_TYPE, DISTRIBUTION_TYPE, RECURRING_SCHEDULE_TYPE, RECURRING_FREQUENCY); PAYMENT_METHOD legitimately has none (P1/registry-srs-fin.md:42 — 'registered, no dedicated column this v1'). The remaining three are the state/status columns: they are excluded from every request DTO (system-assigned, see FiscalYearCreateRequest and JournalEntryCreateRequest javadoc) and are written by FIN from hardcoded String literals in the domain/service layer (11 occurrences of 'DRAFT'/'POSTED'/'VOID'/'OPEN'/'CLOSED'/'HARD_CLOSE'/'YEAR_END_CLOSE' under src/main/java/com/erp/fin), never checked against MDL. No client-writable lookup-backed column is unvalidated, so this is not an exploitable hole today, but the literals FIN writes are not guaranteed to be active values of their MDL type: if MDL's FISCAL_YEAR_STATUS/PERIOD_STATE/JOURNAL_STATUS seed drifts or a value is deactivated, FIN silently persists a code MDL no longer recognises and no test detects it.",
      "resolution": "RESOLVED (INT-R) — NOT a gap; XM-FIN-001 governs client-submitted codes only, and these three are system-assigned state. Grounds: (1) INT-C.md's XM-FIN-001 Contract reads 'data required = the submitted code exists as an active value under the named type ... fallback if absent = reject with FIN-400-INVALID-LOOKUP', and its Interface line triggers 'at the point each lookup-backed field is written or offered as a select-list'. A 'reject the submitted code' fallback is incoherent for a value no client submits: FIN cannot answer its own state transition with a 400 client-validation error. (2) Client-reachability re-checked in code at INT-R: there is no setStatusCode anywhere under src/main/java/com/erp/fin, no request DTO carries statusCode (FiscalYearCreateRequest:16, JournalEntryCreateRequest:19 and EventEntryBuildRequest:39 all list it among the excluded system-assigned fields; JournalEntryService:149 states 'statusCode is system-assigned and never submitted'), and every write is an entity-owned transition from a compile-time constant (JournalEntry.STATUS_DRAFT/POSTED/VOID at JournalEntry.java:95-101,150,212,222; FiscalYear.STATUS_OPEN/CLOSED at FiscalYear.java:59-62,96,124; FiscalPeriod.STATUS_OPEN/SOFT_CLOSE/HARD_CLOSE/YEAR_END_CLOSE at FiscalPeriod.java:64-73,119). The only client-facing exposure is JournalEntrySearchRequest's statusCode EXACT filter — a read predicate, not a write, and an unknown value there returns an empty page, not a bad write. (3) SRS A7 (srs-fin.md:1067-1090) models all three as state machines whose transitions are driven by REQ-FIN-017/028/032/033/034/036 — system actions — and A6/registry-srs-fin.md frame these keys as FIN-owned value lists backing those states, not as client input. Conclusion: the nine client-submitted keys are validated, PAYMENT_METHOD has no column this v1, and these three are out of XM-FIN-001's scope. No per-write validation is added. RESIDUAL RISK (carried forward, unchanged from the INT-C finding): FIN writes these literals unconditionally, so if MDL's FISCAL_YEAR_STATUS / PERIOD_STATE / JOURNAL_STATUS seed drifts or a value is deactivated, FIN keeps persisting a code MDL no longer recognises and nothing fails loudly. That is a seed-consistency concern between FIN's entity constants and MDL's seed data — to be handled by a later phase (ALIGN-BE / seed reconciliation), not by a per-write lookup call.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "SEC-BE",
      "sub": "SEC-BE",
      "endpoint": "PERM_FIN_PERIODS_CLOSE_APPROVE (API-FIN-026 hard-close, API-FIN-027 year-end close) — which role holds it; and PERM_FIN_PERIODS_VIEW — the FIN_PERIODS/VIEW matrix cell with no endpoint or constant",
      "detail": "Two related holes in SEC-BE.md's matrix. (1) GRANT OWNER UNNAMED: SEC-BE.md says PERM_FIN_PERIODS_CLOSE_APPROVE and PERM_FIN_JOURNAL_ENTRIES_CREATE 'must never be held by the same role', and srs-fin.md's Access summary says close-approve is 'held by a role distinct from PERM_FIN_JOURNAL_ENTRIES_CREATE' — but no FIN artifact (P1, P3_1, SEC-BE.md, _SECTIONS.md) names that role, and the only role this schema seeds is SYS_ADMIN (V17), which must hold PERM_FIN_JOURNAL_ENTRIES_CREATE. Granting both to SYS_ADMIN would make FinSeparationOfDutiesService.resolveFacts() see the two user sets intersect and FiscalPeriodDomain.assertCanHardClose throw FIN-403-SOD-VIOLATION on every hard-close and year-end close, permanently. (2) FIN_PERIODS/VIEW: the matrix marks it ✓ but names no API-FIN id, FIN exposes no FiscalPeriod read endpoint, and PermissionConstants declares no PERM_FIN_PERIODS_VIEW — yet the stated gateway convention ('every non-VIEW permission requires VIEW on the same screen first') makes it the prerequisite of the screen's CREATE/UPDATE/CLOSE_APPROVE.",
      "resolution": "RESOLVED for (1), STILL OPEN for (2). (1) GRANT OWNER — DECIDED AND IMPLEMENTED, superseding the earlier 'blocked pending spec clarification': migration V27__fin_close_approver_role.sql mints a dedicated SEC_ROLE 'FIN_CLOSE_APPROVER' holding, by construction, exactly a FIN module grant, a FIN_PERIODS screen grant, and two action grants — PERM_FIN_PERIODS_CLOSE_APPROVE plus the PERM_FIN_PERIODS_VIEW gateway row MenuService.effectiveAuthorityCodes() requires for the first to survive into the caller's authorities — and deliberately NOT PERM_FIN_JOURNAL_ENTRIES_CREATE nor any other FIN screen or action, so RULE-FIN-015 holds structurally rather than by administrative convention. V27 assigns the role to NO user on purpose: resolveFacts() reports entryCreatePermissionShared across each user's role union, so assigning it to the bootstrap 'admin' (SYS_ADMIN, which V25 granted entry creation) would trip FIN-403-SOD-VIOLATION on every close; picking the right non-creator is environment-specific and stays out of migration history. Until such an assignment exists API-FIN-026/027 still answer FIN-403-SOD-VIOLATION (nobody holding close-approval fails the check as surely as one user holding both) — expected, documented in V27's header, and not a defect. SEC-BE.md now states all of this; the near-duplicate ALIGN-BE entry records the same decision. (2) PERM_FIN_PERIODS_VIEW — GROUNDED AS NOT SPURIOUS, but one half left open. Not spurious: the row is load-bearing, V27 must grant it or close-approval is filtered out of the authorities, so it is a real gateway and the matrix ✓ is correct; SEC-BE.md's matrix cell now reads '✓ (no API — gateway row only)' and a new paragraph explains it. It remains the one of 26 registered action rows with no PermissionConstants constant, because no @PreAuthorize references it, because FIN publishes no fiscal-period read endpoint. STILL OPEN, precise question for a human: SCR-REQ-FIN-007's §B2 names period list filters (fiscalYearId EXACT, statusCode EXACT) that no API serves — §B5 and the API registry define none, and the screen currently gets its period rows only from API-FIN-023's create response, so a user cannot list the periods of an existing fiscal year at all. Is that acceptable for v1 (in which case PERM_FIN_PERIODS_VIEW stays a constant-less gateway and srs-fin.md §B2's filters should be struck), or is a POST /api/v1/fin/fiscal-periods/search endpoint missing and to be added as API-FIN-033 with a matching PERM_FIN_PERIODS_VIEW constant? This cannot be settled from the artifacts: the SRS asks for the filters and the API registry does not provide them, and neither is obviously the error. No endpoint was invented. === UPDATE 2026-09-12 (governance-state alignment session) — part (2) is now RESOLVED; part (1) stands as recorded above. The question this entry left for a human — \"is a POST /api/v1/fin/fiscal-periods/search endpoint missing and to be added as API-FIN-033 with a matching PERM_FIN_PERIODS_VIEW constant?\" — was answered YES and BUILT. Delivered, verified by reading each file: FiscalPeriodController.java:68 publishes @PostMapping(\"/search\") on base path /api/v1/fin/fiscal-periods (the controller's own javadoc names it API-FIN-033); FiscalPeriodService.java:176 gates it with @PreAuthorize on PermissionConstants.PERM_FIN_PERIODS_VIEW; and that constant now exists at src/main/java/com/erp/sec/permission/PermissionConstants.java:248 — which is the precise defect this entry recorded, PERM_FIN_PERIODS_VIEW being the one registered SEC_ACTION_REG row with no PermissionConstants constant. NO MIGRATION WAS NEEDED and none was written: the FIN_PERIODS/VIEW registry row was already seeded by V24__fin_security_seed.sql and already granted to SYS_ADMIN by V25__fin_role_grants.sql and to FIN_CLOSE_APPROVER by V27__fin_close_approver_role.sql — only the Java constant and the endpoint were missing. Re-measured after the change: 27 PERM_FIN_* constants against 27 registered FIN action rows (26 from V24 + 1 from V28), an exact set match in both directions, and every one of the 27 referenced from a com.erp.fin service. The 'constant-less gateway' state described above is therefore ended; srs-fin.md §B2's period filters now have an endpoint that serves them and no longer need striking. The alternative this entry offered — accept it for v1 and strike the filters — was NOT taken. === UPDATE 2026-09-12 (final governance-state pass) — part (1) IS NOW RESOLVED TOO, by a different route than the one recorded above, and the reasoning above is SUPERSEDED rather than merely extended. The whole apparatus this entry built around the grant owner assumed a runtime that no longer exists: FinSeparationOfDutiesService.resolveFacts() and FiscalPeriodDomain.assertCanHardClose(...) were DELETED on 2026-09-12 by a recorded human decision. Re-measured, not inferred: `ls src/main/java/com/erp/fin/service/FinSeparationOfDutiesService.java` returns 'No such file or directory' and `grep -rn assertCanHardClose src/main/java/` returns only past-tense javadoc notes, no method. The grounds for the deletion: RULE-FIN-015 (srs-fin.md:1026-1033, re-read today) requires only that the close-approval action be gated by a PERMISSION distinct from the journal-entry-creation permission, enforced through the Security module — REQ-FIN-038 and AC-FIN-038 say the same — and the delivered @PreAuthorize(PERM_FIN_PERIODS_CLOSE_APPROVE) on FiscalPeriodService.hardClose and FiscalYearService.yearEndClose IS that enforcement. The deleted code enforced something stricter and unstated: GLOBAL USER-SET DISJOINTNESS, refusing the close for EVERY caller whenever any single user anywhere held both codes. CONSEQUENCE FOR THIS ENTRY: 'granting both to SYS_ADMIN would trip FIN-403-SOD-VIOLATION on every close' is NO LONGER TRUE — nothing reads the user sets any more — so the reason V25 withheld PERM_FIN_PERIODS_CLOSE_APPROVE from SYS_ADMIN has evaporated, and withholding it now only locks the bootstrap admin out of API-FIN-026/027. V30__fin_sys_admin_close_approve_grant.sql therefore grants that one permission to SYS_ADMIN forward, reversing V25:111's deliberate exclusion (`AND a.PERMISSION_CODE <> 'PERM_FIN_PERIODS_CLOSE_APPROVE'`). V30 is a GRANT ONLY — one INSERT INTO SEC_ROLE_ACTION_GRANT selecting SYS_ADMIN against a.PERMISSION_CODE = 'PERM_FIN_PERIODS_CLOSE_APPROVE' — it registers no action row, so the 27 PERM_FIN_* constants / 27 registered action rows figure is unchanged (re-measured today as an exact 27/27 set match in both directions). STATED HONESTLY: V30 is WRITTEN BUT NOT YET APPLIED — `git status --short src/main/resources/db/migration/` returns it as untracked (`??`) today — so this is closed against the repository, not against a running database. V27__fin_close_approver_role.sql and its FIN_CLOSE_APPROVER role are NOT revoked and stay valid; a deployment that prefers a dedicated approver role over SYS_ADMIN still has one. NOTHING HERE AUTHORISES RESTORING THE SoD SERVICE: FiscalPeriodDomain.java:110-117 carries an explicit 'DO NOT RESTORE THE PREVIOUS IMPLEMENTATION' note, and a side effect of the deletion — FIN-403-SOD-VIOLATION left as a live constant with no throw site — is recorded as its own api_doc_gaps entry below.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "POST /api/v1/fin/journal-entries/{id}/reverse (API-FIN-021) and every reversing recurring template run (API-FIN-014) — RULE-FIN-011/013, ACCOUNTING §12 points 7 and 9",
      "detail": "LEDGER-BREAKING. The reversal flow does BOTH halves of two mutually exclusive designs: JournalEntryService.reverse (:254-263) posts the mirrored entry (JournalPostingService:159 leaves it POSTED) AND sets the original to VOID (JournalEntry.voidByReversal, entity/JournalEntry.java:220-223). Every balance read filters statusCode = POSTED only (JournalLineRepository:111 aggregateAccountBalances, :70 ledger; JournalLineDimensionRepository dimension aggregate; AllocationRuleService:260-262; FiscalYearService:270-272), so the original's lines vanish from every report while the mirror remains. Net ledger effect of a reversal is MINUS the original, not zero. Reproduce: post DR Cash 1000 / CR Revenue 1000, then reverse it; the trial balance afterwards shows Cash CR 1000 and Revenue DR 1000 — an invented, inverted transaction. It still balances, so the §12 point-8 'balances by construction' check does not catch it. RecurringTemplateService:250-258 has the same shape, so a REVERSING template's only ledger footprint is the inverse of its accrual. ReportService:44-45 documents the intended semantics ('a VOID one drops out of it the moment its reversal posts') which is correct ONLY if the mirror is not posted.",
      "resolution": "RESOLVED — HUMAN DECISION: option (a) CLASSIC REVERSAL, applied in code and specs. The original entry now STAYS POSTED for good; the mirror reversal is posted alongside it and the two rows stay linked in both directions (originalEntryId / reversalEntryId, DBF-FIN-042/043). Implemented: JournalEntry.voidByReversal(JournalEntry) was reduced to a pure link-setter and renamed JournalEntry.linkReversal(JournalEntry) — it now assigns only reversalEntry and no longer writes statusCode; both call sites were updated (JournalEntryService.reverse, API-FIN-021, and RecurringTemplateService.postReversalInNextPeriod, API-FIN-014 — the second site had the identical defect and is fixed by the same change). No guard changed: RULE-FIN-013 (JournalEntryDomain.assertCanReverse, FIN-409-NOT-POSTED), RULE-FIN-011's mirroring and RULE-FIN-012's period substitution are untouched. Net ledger effect of a reversal is now exactly zero: both entries are POSTED, so both are seen by the POSTED-only reads (JournalLineRepository.aggregateAccountBalances and findAccountLedgerLines, the dimension aggregate, AllocationRuleService and FiscalYearService), and each reversal line carries the opposite directionCode of its original, so the debit and credit sums cancel per account. Side effect, deliberate: RULE-FIN-016 (a POSTED row is never modified) now holds with NO carve-out at all — the only write back to the original is the reversalEntryId link, never its accounting state; the 'sanctioned carve-out' language was removed from JournalEntry, JournalEntryDomain, JournalEntryService and RecurringTemplateService. Specs amended: srs-fin.md §A7 state machine (POSTED is now terminal; the reverse action performs no transition), packages/backend-execution/DATA-DOM/DATA-DOM-TRANSACTIONAL.md and SVC-API/SVC-API-INT.md, and the P3_1/backend-execution-plan-fin.md plan copy of both. Test expectations updated (ids and TC markers unchanged): TC-FIN-028 now asserts the original stays POSTED and that the pair nets to zero, TC-FIN-024 asserts the period-N accrual stays POSTED, TC-FIN-030's precondition became a DRAFT entry. JournalEntry.STATUS_VOID and the JOURNAL_STATUS lookup value 'VOID' now have NO writer anywhere in FIN; both were deliberately RETAINED and nothing was removed from any spec — JOURNAL_STATUS is shared MDL lookup data (XM-FIN-001) and retiring a lookup value is a separate, explicit decision. FOLLOW-UP NOW CLOSED (double-reversal hole): because a reversed entry stays POSTED, RULE-FIN-013's status half stopped catching a second reverse of the same entry, which would have posted a second mirror and left a net ledger effect of -(original). AC-FIN-030 always required that rejection, so no new rule was invented — only the code for an already-stated requirement. Guard: JournalEntryDomain.assertCanReverse() became assertCanReverse(boolean alreadyReversed) — RULE-FIN-013 keeps exactly ONE implementation, now carrying both halves (non-POSTED -> FIN-409-NOT-POSTED; already linked -> FIN-409-ALREADY-REVERSED). The fact is resolved by the service from the loaded row's REVERSAL_ENTRY_ID (DBF-FIN-043) and passed in as a plain argument, so the domain class still touches no repository (A.0.3) and no business-rule if is inlined in a service (A.5.18). Applied to BOTH reversal paths: JournalEntryService.reverse (API-FIN-021) and RecurringTemplateService.postReversalInNextPeriod (API-FIN-014) — the two and only two linkReversal call sites. New Error Catalog row FIN-409-ALREADY-REVERSED | RULE-FIN-013 | API-FIN-021, 014 | 409, added to packages/backend-execution/_SECTIONS.md and the P3_1/backend-execution-plan-fin.md copy; 409/Status.CONFLICT rather than 422 because the block comes from the existence of a specific referencing record (the reversal entry already pointing at this one), which is the taxonomy's CONFLICT row. Constant appended to FinErrorCodes and the message added to both i18n bundles (32 FIN keys each, parity kept). SRS amended (narrow correction of the two lines the classic-reversal decision made untrue): srs-fin.md line 682 rationale now reads 'prevents double-reversal (an entry that already carries a reversal link) or reversing a DRAFT row', and AC-FIN-030's Given at line 686 now reads 'an entry already reversed once (it stays POSTED and carries a reversalEntryId link to its reversal)'. The same claim was corrected in DATA-DOM/DATA-DOM-TRANSACTIONAL.md, SVC-API/SVC-API-INT.md (Validations/Errors/Orchestration), the QR-FIN-035 and RULE-to-code coverage lines in _SECTIONS.md, the P3_1 plan copy of all of the above, and test_gen/test-execution-manifest-fin.md's CRUD matrix ('VOID via reverse' -> no VOID path). Test coverage restored as a linked-entry precondition instead of a VOID one: NEW TC-FIN-048 (RULE-FIN-013 -> FIN-409-ALREADY-REVERSED) in packages/backend-test/RULE-SCENARIOS.md and test_gen/backend-test-plan-fin.md, registered in the traceability index, the coverage note, backend-test/state.json markers and the test-execution manifest; TC-FIN-030 keeps its id, marker and DRAFT precondition and now points at TC-FIN-048. No existing TC id or TC marker was changed or removed.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "ABSENT",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "XM-FIN-001 — all 13 FIN-owned MDL lookup types; blocks API-FIN-002/006/007/010/011/013/016/019/020 on a fresh deployment",
      "detail": "No migration seeds ANY FIN lookup type or value into MDL_LOOKUP_TYPE / MDL_LOOKUP_VALUE. The only INSERTs into MDL_LOOKUP_TYPE in the whole repo are V20__notif_file_lookup_data_migration.sql:66-78, which seeds NOTIF/FILE types only. MdlLookupApiImpl.readActiveValuesByKey (crossmodule/MdlLookupApiImpl.java:53-58) throws MDL-404-TYPE-KEY when the key is absent or inactive; FinLookupValidationService.assertValidCode (:114-125) relabels exactly that to FIN-400-INVALID-LOOKUP with Status.VALIDATION_ERROR. Consequence on a fresh database: POST /api/v1/fin/accounts fails with HTTP 400 / FIN-400-INVALID-LOOKUP on accountTypeCode before any account can exist, and the same holds for every create that carries a lookup-backed column (ACCOUNT_TYPE, DEBIT_CREDIT, JOURNAL_TYPE, ACCOUNTING_EVENT_TYPE, ACCOUNT_DERIVATION_TYPE, AMOUNT_SOURCE_TYPE, DISTRIBUTION_TYPE, RECURRING_SCHEDULE_TYPE, RECURRING_FREQUENCY). FIN is therefore entirely unusable out of the box — a strictly larger blocker than the two already recorded, and it was never noticed because INT-C/INT-R only verified the CALL, never that the data it reads exists.",
      "resolution": "HUMAN DECISION APPLIED (final) — shape (a): FIN authors its own forward migration seeding MDL's tables, mirroring the precedent V20 set (NOTIF/FILE seed their own lookup types straight into MDL_LOOKUP_TYPE/MDL_LOOKUP_VALUE) and the one V24 set (FIN seeds its own screens straight into SEC's registry tables). Shape (b) is rejected: an undocumented manual onboarding step would leave every fresh deployment and every backend test run broken until someone remembered it. Delivered as src/main/resources/db/migration/V26__fin_mdl_lookup_seed.sql — 13 MDL_LOOKUP_TYPE rows, all OWNER_MODULE_CODE='FIN', plus 36 MDL_LOOKUP_VALUE rows, all active, PKs from SEQ_MDL_LOOKUP_TYPE/SEQ_MDL_LOOKUP_VALUE, every FK resolved by natural key on KEY, bilingual NAME_AR/NAME_EN on every row, SORT_ORDER following srs-fin.md A6 listing order (which for the three status keys is also their A7 lifecycle order). A DO-block guard aborts the migration if SEC_MODULE_REG has no CODE='FIN' row (RULE-MDL-001 / XM-MDL-001 SOFT-READ; V24 supplies it). Per-key value counts: ACCOUNT_TYPE 5, DEBIT_CREDIT 2, PERIOD_STATE 4, FISCAL_YEAR_STATUS 2, JOURNAL_TYPE 5, JOURNAL_STATUS 3, ACCOUNT_DERIVATION_TYPE 3, AMOUNT_SOURCE_TYPE 3, DISTRIBUTION_TYPE 3, RECURRING_SCHEDULE_TYPE 2, RECURRING_FREQUENCY 4 — identical in srs-fin.md A6, P0/module-registry-fin.md LOOKUPS OWNED and P1/registry-srs-fin.md's count column, with no disagreement between the three. NOT FULLY GROUNDED — two keys seeded as TYPE ONLY, with ZERO values, because no artifact enumerates any: ACCOUNTING_EVENT_TYPE (A6: 'none seeded — host-specific, added as data [§3, §6.4]') and PAYMENT_METHOD (A6: 'none seeded — host-specific'; registry-srs-fin.md:42 adds 'registered, no dedicated column this v1'). Nothing was invented for them; the types exist so MDL returns an empty active-value list rather than MDL-404-TYPE-KEY. Residual consequence to carry forward: FIN_EVENT_TYPE_RULE.event_type_code validates against ACCOUNTING_EVENT_TYPE, so creating an event-type rule (API-FIN-010) still requires the host to add its own event-type values through MDL's lookup administration first — a documented host-specific deployment step, not a defect of this seed, and the one place a backend test scenario must seed its own value. Also not grounded by any artifact: the per-VALUE bilingual display labels (A6 lists codes only) — written as the standard Arabic/English accounting terms for each code, exactly as V20 did for NOTIF/FILE; TYPE-level labels are verbatim from module-registry-fin.md. VERIFIED: the migration was executed against the live erp_db schema inside a transaction that was then rolled back — 13 type rows and 36 value rows inserted with no error; a follow-up query confirmed all 13 keys FinLookupValidationService declares as KEY_* constants resolve to an active FIN-owned type, and all 32 distinct status literals FIN's entities/domains/services write (ASSET..WEEKLY, grepped from src/main/java/com/erp/fin) resolve to an active value row under their own key — zero missing keys, zero unmatched literals. mvn -DskipTests compile and mvn test both BUILD SUCCESS (8/8 tests). Test-phase impact cleared: FIN API scenarios can now create lookup-backed rows on a fresh database, with the ACCOUNTING_EVENT_TYPE caveat above.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "PATCH /api/v1/fin/fiscal-periods/{id}/hard-close (API-FIN-026) and POST /api/v1/fin/fiscal-years/{id}/year-end-close (API-FIN-027) — the documented repair path for PERM_FIN_PERIODS_CLOSE_APPROVE does not work",
      "detail": "V25's header (lines 56-61) prescribes the repair for the deliberately-ungranted close-approval permission: a human creates a close-approver role through SEC's live role administration and assigns it to a user who does not hold PERM_FIN_JOURNAL_ENTRIES_CREATE. That repair CANNOT succeed. JwtAuthenticationFilter.passesGateway (src/main/java/com/erp/sec/security/JwtAuthenticationFilter.java:132-141) splits a permission code at lastIndexOf('_') and requires the sibling <prefix>_VIEW to be held. For PERM_FIN_PERIODS_CLOSE_APPROVE the last underscore precedes APPROVE, so the filter demands PERM_FIN_PERIODS_CLOSE_VIEW — a code that exists in no registry row, no constant and no migration. toAuthorities (:121-128) therefore strips the authority even after a valid grant, and both endpoints answer 403 permanently. This is the ONLY FIN permission with a two-token action; every other multi-word FIN code splits correctly because the extra words sit in the screen segment. V24:39-43 and V25:64-67 both assert the gateway convention holds on every screen; for FIN_PERIODS/CLOSE_APPROVE it does not.",
      "resolution": "HUMAN DECISION RECORDED (final) — RESOLVED by V27__fin_close_approver_role.sql. The SEC gateway defect this entry described is fixed independently: JwtAuthenticationFilter no longer splits a permission code at lastIndexOf('_'); MenuService.effectiveAuthorityCodes() now applies RULE-SEC-007 against the registry's own screen rows (RoleActionGrantDomain.isGatewayAction), so PERM_FIN_PERIODS_CLOSE_APPROVE survives authority resolution as long as the SAME screen also carries a granted VIEW row. A grant therefore now takes effect. Decision on the missing grant owner: rather than granting close-approval to SYS_ADMIN (which holds PERM_FIN_JOURNAL_ENTRIES_CREATE and would make the close permanently impossible), V27 mints a DEDICATED role FIN_CLOSE_APPROVER ('معتمد إقفال الفترات المالية' / 'Fiscal Period Close Approver'), code shape following the seeded <SCOPE>_<FUNCTION> convention (SYS_ADMIN, CU_ADMIN, FILE_ADMIN, NOTIF_ADMIN). It holds exactly: Tier-1 module FIN, Tier-2 screen FIN_PERIODS only, Tier-3 two action rows — PERM_FIN_PERIODS_VIEW (the gateway row V24 registered for this purpose) and PERM_FIN_PERIODS_CLOSE_APPROVE. It deliberately holds NOTHING else: not PERM_FIN_JOURNAL_ENTRIES_CREATE, not FIN_PERIODS CREATE/UPDATE, no other FIN screen. RULE-FIN-015 is thus satisfied by construction — the role cannot be assigned in a way that also confers entry creation through it. TWO OPERATIONAL PRECONDITIONS remain, documented in V27's header and NOT resolvable by migration: (1) V27 assigns the role to NO USER, and FinSeparationOfDutiesService.resolveFacts() reports closeApprovePermissionHeld = !approvers.isEmpty() while FiscalPeriodDomain.assertCanHardClose throws FIN-403-SOD-VIOLATION when that is false — so 'nobody holds close-approval' fails the close exactly as 'one user holds both' does; a real SEC_USER_ROLE assignment is still required before API-FIN-026/API-FIN-027 work. (2) The assignee MUST be a user holding no role that carries PERM_FIN_JOURNAL_ENTRIES_CREATE. The bootstrap 'admin' user holds SYS_ADMIN, which V25 granted that permission; since resolveFacts() compares the two user sets across each user's ROLE UNION, assigning FIN_CLOSE_APPROVER to 'admin' would put 'admin' in both sets and trip FIN-403-SOD-VIOLATION on every hard-close and year-end close. 'admin' is therefore deliberately NOT assigned. Verified against the live erp_db schema inside BEGIN/ROLLBACK: 1 role row, 1 module grant, 1 screen grant, 2 action grants, 0 user assignments; users holding PERM_FIN_PERIODS_CLOSE_APPROVE = {} and users holding PERM_FIN_JOURNAL_ENTRIES_CREATE = {admin} — disjoint. === UPDATE 2026-09-12 (final governance-state pass) — BOTH REMAINING OPERATIONAL PRECONDITIONS ARE GONE, and they are gone because the mechanism that created them was deleted, not because either was satisfied. Precondition (1) read 'V27 assigns the role to NO USER, and FinSeparationOfDutiesService.resolveFacts() reports closeApprovePermissionHeld = !approvers.isEmpty() while FiscalPeriodDomain.assertCanHardClose throws FIN-403-SOD-VIOLATION when that is false'. Precondition (2) read 'the assignee MUST be a user holding no role that carries PERM_FIN_JOURNAL_ENTRIES_CREATE'. NEITHER SENTENCE DESCRIBES THE CODE ANY MORE. Verified today: src/main/java/com/erp/fin/service/FinSeparationOfDutiesService.java does not exist; FiscalPeriodDomain has no assertCanHardClose method (FiscalPeriodDomain.java:93 states outright 'there is deliberately NO assertCanHardClose(...) method here'); and `grep -rn 'FIN_403_SOD_VIOLATION' src/main/java/` finds no throw site anywhere. RULE-FIN-015 is now satisfied by the @PreAuthorize(PERM_FIN_PERIODS_CLOSE_APPROVE) gate alone, which is exactly what the rule's own Statement asks for and no more. So an empty approver set no longer fails the close, and a user holding both permissions no longer fails it for everyone else — the close simply requires the permission, like every other gated operation in the platform. THE SEC GATEWAY FIX recorded above is unaffected and still stands: MenuService.effectiveAuthorityCodes() still resolves the gateway against the registry's own screen rows, so PERM_FIN_PERIODS_CLOSE_APPROVE still survives authority resolution only when the same screen also carries a granted VIEW row — which is why V30, granting close-approval to SYS_ADMIN, works: V25 already granted SYS_ADMIN the FIN_PERIODS VIEW row. See the SEC-BE entry above for V30 in full. V27 and FIN_CLOSE_APPROVER remain valid and are not revoked; assigning that role to a user is now an ordinary administrative choice rather than a precondition for the endpoints to function at all.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "POST /api/v1/fin/journal-entries/from-event (API-FIN-020) — RULE-FIN-010 remainder computation, and RULE-FIN-003's guard keying off a different column than the builder",
      "detail": "Two related defects in remainder/percentage distribution (ACCOUNTING §12 point 6). (1) SIDE-BLIND REMAINDER: EventEntryService:135-153 accumulates every non-remainder line's amount into one flat 'assigned' total regardless of directionCode, then EventTypeRuleDomain.remainderAmount (:220-224) computes baseAmount - assigned. With base 1000 and lines DEBIT/FIELD=1000, CREDIT/PERCENTAGE 60%=600, CREDIT/REMAINDER, assigned = 1600 and the remainder computes to -600. The negative amount then trips CHK_FIN_JOURNAL_LINE_AMOUNT_POSITIVE (V22:425) and surfaces as an unlocalized 409 DATA_INTEGRITY_VIOLATION from GlobalExceptionHandler:79-87, not FIN-409-UNBALANCED. The remainder must be computed per side (debit total so far vs credit total so far). (2) GUARD/BUILDER COLUMN MISMATCH: EventTypeRuleDomain.assertRemainderLineSetValid (:109-126) only fires when some sibling line has distributionTypeCode = PERCENTAGE, and counts isRemainderFl; EventEntryService:137-143 decides remainder-ness from amountSourceTypeCode = REMAINDER. Two lines with amountSourceTypeCode = REMAINDER and distributionTypeCode = FIXED pass RuleLineService:76 unchallenged; at run time the first absorbs the whole residue and the second computes to 0, again dying on the DB CHECK. AllocationRuleDomain.assertRemainderTargetSetValid (:89-91, isRemainderFl) vs AllocationRuleService:222-227 (distributionTypeCode = REMAINDER) has the identical split. Rounding itself is correct where one remainder line exists: percentages use divide(100, scale 4, HALF_UP) and the remainder is an exact subtraction, so the residue is absorbed.",
      "resolution": "RESOLVED — implemented in code and propagated to the specs. (1) PER-SIDE REMAINDER: EventTypeRuleDomain.remainderAmount now takes (opposingSideTotal, ownSideTotalSoFar) and EventEntryService.buildLines accumulates through the new JournalEntryDomain.SideTotals record, so the remainder is the difference between the two posting sides, never between a base amount and every line regardless of direction. Worked case from the detail (base 1000; DEBIT/FIELD 1000, CREDIT/PERCENTAGE 60% = 600.0000, CREDIT/REMAINDER): old −600.0000 → DB CHECK violation; new 400.0000 and the entry balances D1000 = C600+400. AC-FIN-012 (100 as 33%+33%+remainder against a DEBIT 100) now yields the AC's own 34.0000, where the side-blind form yielded −66.0000. A non-positive residue is no longer an opaque DATA_INTEGRITY_VIOLATION but the new FIN-422-REMAINDER-NOT-POSITIVE. The allocation path is unchanged arithmetically — its two arguments already WERE the opposing side's total (the source line) and its own side's running total. (2) SINGLE MARKER: isRemainderFl (DBF-FIN-103/141) is now the only marker. EventTypeRuleDomain.isRemainderLine / AllocationRuleDomain.isRemainderTarget are read by both builders; sourcedAmount and targetAmount return a plain BigDecimal and refuse a REMAINDER-typed line (FIN-422-REMAINDER-MARKER); and the RULE-FIN-003 guards now (a) reject any disagreement between isRemainderFl and the line's/target's own REMAINDER type code and (b) fire on 'any percentage sibling OR any line already marked remainder', so the two FIXED/REMAINDER lines of the detail are rejected at configuration time with FIN-409-REMAINDER-COUNT instead of dying on CHK_FIN_JOURNAL_LINE_AMOUNT_POSITIVE at run time. Specs: srs-fin.md §A5 RULE-FIN-003 and RULE-FIN-010 statements amended (the only two SRS rule edits made); Error Catalog + RULE→code coverage (both copies); API-FIN-011/016/017/020 Validations, Errors and Orchestration; DATA-DOM ENT-FIN-010/014. NOTE FOR OPERATIONS: any already-stored rule line or allocation target whose isRemainderFl disagrees with its type code now fails loudly at run time with FIN-422-REMAINDER-MARKER instead of silently mis-distributing — deliberate, and the ledger-safe direction of failure.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "POST /api/v1/fin/journal-entries (API-FIN-019) — no coherence check between fiscalYearId, periodId and docDate",
      "detail": "JournalEntryService.createManual (:159-180) resolves the fiscal year and the period by id INDEPENDENTLY and never checks that the period belongs to that year, nor that docDate falls within the period's start/end dates (DBF-FIN-080/081, both NOT NULL and available). Only checkTargetPeriodOpen runs. A caller may therefore post an entry stamped fiscalYearId = FY2025, periodId = an open period of FY2024, docDate = 2019-01-01. That row then appears in the FY2025 trial balance when scoped by periodId (aggregateAccountBalances filters period OR year, independently), is invisible to the balance sheet's asOfDate cut-off, and corrupts FiscalYearService.balancesOf (:270-272, scoped purely by journalEntry.fiscalYear) — that is, the year-end closing and opening entries themselves. The docNo counter is also scoped by the submitted year (:365-374), so the entry consumes a number in the wrong year's series. No RULE, AC or API block in P1/P2/P3_1 states this coherence requirement, which is why no sub implemented it.",
      "resolution": "RESOLVED — implemented; the rule text was derived rather than waiting on confirmation, and is flagged below for human ratification. New RULE-FIN-017 (srs-fin.md §A5, appended after RULE-FIN-016; no existing RULE id touched): the submitted periodId must belong to the submitted fiscalYearId (DBF-FIN-076) and docDate must fall inside that period's [startDate, endDate] (DBF-FIN-080/081). Implemented as JournalEntryDomain.assertHeaderCoherent(...) — a domain guard, not an inlined service if (A.5.18) — called fail-fast from JournalEntryService.createManual right after the year and period are resolved, before RULE-FIN-006/007/008/009 are collected: an incoherent header makes the period gate itself meaningless. Two new catalog rows rather than a fold into FIN-404-PERIOD / FIN-409-PERIOD-NOT-OPEN, which say something factually different (the period exists and may well be open): FIN-400-PERIOD-NOT-IN-YEAR and FIN-400-DOCDATE-OUTSIDE-PERIOD, both 400/VALIDATION_ERROR because nothing in the stored ledger is in conflict — the request's own three header fields are mutually inconsistent, the taxonomy's structural row. Scope is API-FIN-019 only, deliberately: every system-generated path derives the three facts from one another (resolvePeriodContaining takes the period FROM the date and the year FROM the period; API-FIN-027 uses the year's own last/first period and its own end/start date), and adding the guard to the shared pipeline would wrongly reject a RULE-FIN-012 reversal whose substituted open period does not contain today's date. The noted createManual/JournalPostingService duplication is untouched — a separate refactor, not this fix. HUMAN RATIFICATION WANTED on the RULE-FIN-017 wording and on 400 vs 422 for its two codes.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "ENT-FIN-004.docNo (DBF-FIN-035) — concurrent journal-entry creation; and API-FIN-021 concurrent reversal",
      "detail": "Two unguarded read-then-write races, neither noted by any earlier sub. (1) DOC NUMBER: JournalDocNoGenerator.next (numbering/JournalDocNoGenerator.java:19-21) computes highest-existing + 1, and both call sites (JournalEntryService:173, JournalPostingService:140) read the current maximum with a plain query — no sequence, no lock, no retry. Two concurrent creates in the same fiscal year compute the same docNo and the second violates UQ_FIN_JOURNAL_ENTRY_YEAR_DOCNO (V22:421), surfacing as an unlocalized 409 DATA_INTEGRITY_VIOLATION. The recorded docNo decision chose a per-fiscal-year counter backed by that constraint but specified no allocation mechanism. (2) REVERSAL: no @Version on AuditableEntity or any FIN entity and no @Lock on JournalEntryRepository.findOneWithLines (:65-67), so two concurrent POST /{id}/reverse both read POSTED, both post a mirror, and the second save(original) silently overwrites reversalEntryId — one original, two posted mirrors, one orphaned. Sequential double reversal IS correctly rejected by assertCanReverse. (3) Same shape, benign: the RULE-FIN-004 duplicate-event exists-check (EventEntryService:93-95) is backed by UQ_FIN_JOURNAL_ENTRY_EVENT_REF (V22:422), so the ledger is safe, but a concurrent duplicate returns generic 409 DATA_INTEGRITY_VIOLATION instead of FIN-409-DUPLICATE-EVENT.",
      "resolution": "RESOLVED for (1) and (2); (3) recorded as out of scope. (1) DOC NUMBER: allocation is now serialized by a PESSIMISTIC_WRITE row lock on the owning FIN_FISCAL_YEAR row — FiscalYearRepository.lockForDocNoAllocation(fiscalYearPk), taken before the highest-docNo read and held to commit, in the only two places a docNo is allocated (JournalEntryService.createManual and JournalPostingService.buildValidateAndPost). Two concurrent creates in one fiscal year can therefore no longer observe the same predecessor: the second blocks, then reads the first's committed number and takes the next one. UQ_FIN_JOURNAL_ENTRY_YEAR_DOCNO stays the authoritative backstop but becomes unreachable in practice. A per-year DB sequence was rejected (the counter restarts each fiscal year and the schema declares none, and writing one would need a migration this sub may not write); catch-and-retry was rejected outright — build-create-service forbids a service catching DataIntegrityViolationException. (2) REVERSAL: JournalEntryRepository.lockForReversal(id), a PESSIMISTIC_WRITE read of the entry header, is taken in JournalEntryService.reverse before JournalEntryDomain.assertCanReverse evaluates the 'already reversed' fact, making the read-then-write on REVERSAL_ENTRY_ID atomic; the second concurrent reversal now gets FIN-409-ALREADY-REVERSED, exactly what a sequential repeat gets. Header only, because FOR UPDATE cannot be applied to the nullable side of findOneWithLines' outer join; the lines are loaded afterwards and resolve to the same managed instance. No @Version was added — it would be an entity-wide change affecting every FIN write path for one guard. Classic reversal is untouched: the original stays POSTED and no VOID is written anywhere. (3) CONCURRENT DUPLICATE EVENT — NOT CLOSED, and deliberately so: the ledger is already safe (UQ_FIN_JOURNAL_ENTRY_EVENT_REF), and the only fix for the opaque 409 is to map DataIntegrityViolationException onto the FIN codes in com.erp.common.web.GlobalExceptionHandler, which is shared platform code outside this work's scope — a service is forbidden from catching it locally. Needs a platform decision.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "Query Reference Catalog (QR-FIN-001..044) — four dead repository methods, five uncatalogued queries, three catalog cross-reference errors",
      "detail": "Re-running the QRC self-check in BOTH directions, as ALIGN requires, contradicts the block's own claim. (a) DEAD CODE (contract rule A.2.9, which gov-validate-backend-feature rejects): FiscalPeriodRepository:30 existsByFiscalPeriodPkAndStatusCode, JournalEntryRepository:56 existsByJournalEntryPkAndStatusCode, JournalLineRepository:49 countByJournalEntryPk and RecurringTemplateRepository:29 findByIsActiveFlAndNextRunDateLessThanEqual have ZERO call sites anywhere in src/. The first two carry javadoc claiming to be the 'cheap pre-check' for QR-FIN-040 and QR-FIN-035 — both claims are false: those QRs are realised by FiscalPeriodDomain.assertCanReopen and JournalEntryDomain.assertCanReverse on the already-loaded entity, with no query at all. (b) NO QR ID: AccountRepository:47 findFirstByIsRetainedEarningsFlTrueOrderByAccountPkAsc, DimensionRepository:27 existsByCode, FiscalYearRepository:30 existsByCode, FiscalYearRepository:41 findByStartDate and FiscalPeriodRepository:39 findByFiscalYearId cite only an API id; no QR covers them, and the catalog has uniqueness QRs for accounts (005) and dimension values (010) but none for ENT-FIN-002 or ENT-FIN-007. (c) CATALOG ERRORS: QR-FIN-022's intent (_SECTIONS.md:304) cites 'via QR-FIN-040' for a source balance — QR-FIN-040 is the hard-closed period check; it should read QR-FIN-043. QR-FIN-016 is scoped to ENT-FIN-010/API-FIN-011 in the table (:298) but to ENT-FIN-010+014/API-FIN-011+016 in the id definition (:452); the code follows the wider one. QR-FIN-042's intent says 'running balance' but the query deliberately returns none (the service accumulates it). Forward direction is clean: all 44 QR ids do have a real implementation, and the 'no join for a lookup label' claim holds (zero MDL joins in any FIN @Query).",
      "resolution": "RESOLVED. Every claim re-verified against src/ before acting. (a) DEAD CODE — all four confirmed to have ZERO call sites anywhere under src/ (main and test) and DELETED: FiscalPeriodRepository.existsByFiscalPeriodPkAndStatusCode, JournalEntryRepository.existsByJournalEntryPkAndStatusCode, JournalLineRepository.countByJournalEntryPk, RecurringTemplateRepository.findByIsActiveFlAndNextRunDateLessThanEqual (with its now-unused java.time.LocalDate / java.util.List imports). The first two carried javadoc calling themselves the 'cheap pre-check' for QR-FIN-040/QR-FIN-035; both claims were false — those QRs are decided by FiscalPeriodDomain.assertCanReopen and JournalEntryDomain.assertCanReverse on the already-loaded entity, with no query. RecurringTemplateRepository's due-template selection served a scheduler the API registry never defines, so deletion was the honest choice; its class javadoc already documents QR-FIN-019 as the inherited findById, and the catalog row now says so too. mvn -DskipTests compile and mvn test (10/10) both pass after the deletions. (b) NO QR ID — all five confirmed real AND called, so all five were catalogued rather than deleted, with new ids that renumber nothing: QR-FIN-045 AccountRepository.findFirstByIsRetainedEarningsFlTrueOrderByAccountPkAsc (FiscalYearService:345, API-FIN-027); QR-FIN-046 DimensionRepository.existsByCode (DimensionService:66, API-FIN-006); QR-FIN-047 FiscalYearRepository.existsByCode (FiscalYearService:96, API-FIN-023); QR-FIN-048 FiscalYearRepository.findByStartDate (FiscalYearService:362, API-FIN-027); QR-FIN-049 FiscalPeriodRepository.findByFiscalYearId (FiscalYearService:172/192, RecurringTemplateService:280, API-FIN-023/027/014). Added to the Query Reference Catalog, the QR id definitions and the owning entities' DATA-DOM REPOSITORY OPS lines, in both the _SECTIONS.md and P3_1 copies; the QRC SUMMARY now reads 49 ids, QR-FIN-001..049. (c) CATALOG ERRORS — all three corrected: QR-FIN-022's intent now cites QR-FIN-043 (the shared balance aggregation) instead of QR-FIN-040 (the hard-closed-period check); QR-FIN-016's table row now carries the wider ENT-FIN-010+014 / API-FIN-011+016 scope its id definition and the code already had; QR-FIN-042's intent no longer claims to return a running balance and states that the service accumulates it. The QRC line of the ALIGN self-check was rewritten to describe exactly this instead of an unqualified ✓.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "DB Alignment Manifest (§4) and db-script-fin.md — count and property-name defects surviving the V23 insertion",
      "detail": "The 147 DBF rows themselves are sound — 147 contiguous ids in db-script-fin.md's matrix and its §6 index, 147 byte-identical rows in BOTH manifest copies (_SECTIONS.md L128-274 and P3_1 L127-273), 147 physical columns across V22+V23 with a 1:1 match in both directions, every one mapped to an entity field, and all 20 index/constraint names identical. Four documentation defects remain. (a) db-script-fin.md contradicts itself: header L7 says '147 DBF', footer L194 says 'Total: 146 DBF ids across 14 tables' — the footer was never bumped when DBF-FIN-147 was inserted at L24. (b) The ALIGN block's own MANIFEST line (_SECTIONS.md:402, and the P3_1 copy at :1196) certifies '12 columns note XM-FIN-001'; the actual count is 16, across 10 entities (DBF-FIN-005, 006, 039, 040, 056, 069, 082, 090, 101, 103, 105, 106, 112, 113, 127, 144), independently confirmed by 16 XM-FIN-001 COMMENT ON COLUMN lines in V22. The self-check certified a wrong number. (c) Five manifest rows name a _pk column as '...Id', breaking the manifest's own stated rule (_SECTIONS.md:65, 'property = camelCase of the db-script column') and disagreeing with the entity: DBF-FIN-098 ruleLineId/ruleLinePk, 109 recurringTemplateId/recurringTemplatePk, 122 recurringTemplateLineId/recurringTemplateLinePk, 130 allocationRuleId/allocationRulePk, 139 allocationTargetId/allocationTargetPk — and DBF-FIN-130's 'allocationRuleId' collides with DBF-FIN-140's real FK of the same name. Identical in both copies. (d) V22:240's COMMENT ON TABLE FIN_ACCOUNT still reads '[DBF-FIN-001..013]'; db-script-fin.md:429 says '[DBF-FIN-001..013, DBF-FIN-147]'. V23 added the column comment but no table comment, so the live DB's FIN_ACCOUNT comment permanently omits DBF-FIN-147 — and V22 is applied and must never be edited, so this can only be corrected forward or accepted.",
      "resolution": "RESOLVED for (a)-(c); (d) ANNOTATED AND LEFT OPEN for a human. (a) db-script-fin.md's footer now reads 'Total: 147 DBF ids across 14 tables' and says why it read 146 (never bumped when DBF-FIN-147 was inserted by V23); the header's '147 DBF' was the correct half and is unchanged. (b) Recounted independently: 16 manifest rows carry XM-FIN-001 (DBF-FIN-005, 006, 039, 040, 056, 069, 082, 090, 101, 103, 105, 106, 112, 113, 127, 144) across 10 entities, matching the 16 XM-FIN-001 COMMENT ON COLUMN lines in V22. Both the manifest preamble and the ALIGN MANIFEST line now read '16 columns across 10 tables', in both copies (_SECTIONS.md and P3_1). (c) All five rows corrected to the entity's own property name, in both copies: DBF-FIN-098 ruleLinePk, 109 recurringTemplatePk, 122 recurringTemplateLinePk, 130 allocationRulePk, 139 allocationTargetPk — which also dissolves the collision between DBF-FIN-130 and DBF-FIN-140's real allocationRuleId FK. Verified against RuleLine.java:63, RecurringTemplate.java:48, RecurringTemplateLine.java:63, AllocationRule.java:56, AllocationTarget.java:64. (d) NOT FIXED — needs a human call, unchanged. V22:240's COMMENT ON TABLE FIN_ACCOUNT is applied and immutable, so the live DB's table comment omits DBF-FIN-147 until someone writes a forward migration purely for a comment. db-script-fin.md now carries an explicit ALIGN-BE note at that line saying the live database does not match, and the ALIGN self-check records it as an open SCHEMA COMMENT finding rather than pretending it is clean. The same treatment was given to V22:278's doc_no comment, which still says 'platform numbering engine' — a component that does not exist. Of the lower-severity notes: the PK-generation divergence is now stated in db-script-fin.md (below the matrix) and in the execution plan's extraction block, which no longer claims IDENTITY PKs — the 14 matrix rows themselves were left as written rather than rewriting applied history. The 20 scalar-Long-vs-@ManyToOne FK rows and AuditableEntity's length-255 createdBy/updatedBy against VARCHAR(100) are recorded as RESIDUAL on the ALIGN MANIFEST line and NOT corrected: the first is a manifest modelling convention question and the second is platform-wide, affecting every module, so neither is FIN's to settle. === UPDATE 2026-09-12 (governance-state alignment session) — (d) UNCHANGED and STILL OPEN; the AuditableEntity RESIDUAL is now HALF-resolved. (d) V22__fin_schema.sql:240's COMMENT ON TABLE FIN_ACCOUNT was re-read today and still reads '[DBF-FIN-001..013]' against db-script-fin.md:447's '[DBF-FIN-001..013, DBF-FIN-147]'; V22 is applied and immutable, nothing delivered today touches it, and it remains an open SCHEMA COMMENT finding in the ALIGN self-check. WHAT WAS DONE about the audit-column residual: the ENTITY DECLARATION was narrowed — com/erp/common/domain/AuditableEntity.java:26 and :32 now declare @Column(length = 100) for CREATED_BY and UPDATED_BY instead of 255, with an in-file comment recording why, so the JPA declaration matches the narrowest live physical width instead of contradicting it. WHAT WAS NOT DONE, stated plainly so this is not read as closed: the PHYSICAL schema is still inconsistent and NO migration was written to reconcile it. Counted directly from src/main/resources/db/migration/ by grepping '(created_by|updated_by) +VARCHAR' and discarding the eight V2 tables that V14__drop_legacy_security_schema.sql drops: 16 LIVE tables define both columns as VARCHAR(100) — SEC_USER (V16:52/54), SEC_ROLE (V16:66/68), SEC_MODULE_REG (V16:78/80), SEC_SCREEN_REG (V16:113/115), SEC_ACTION_REG (V16:127/129), MDL_LOOKUP_TYPE (V18:37/39), MDL_LOOKUP_VALUE (V18:54/56), FIN_DIMENSION (V22:52/54), FIN_ACCOUNT (V22:68/70), FIN_FISCAL_YEAR (V22:81/83), FIN_EVENT_TYPE_RULE (V22:93/95), FIN_RECURRING_TEMPLATE (V22:109/111), FIN_ALLOCATION_RULE (V22:121/123), FIN_DIMENSION_VALUE (V22:137/139), FIN_FISCAL_PERIOD (V22:154/156) and FIN_JOURNAL_ENTRY (V22:174/176) — while 6 define them as VARCHAR(255): CU_APP_CONFIGURATION (V1:25/27), NOTIF_TEMPLATE (V6:30/32), NOTIF_CHANNEL_CONFIG (V6:41/43), NOTIF_LOG (V6:62/64), FILE_CATEGORY (V8:26/28) and FILE_DOCUMENT (V8:47/49). Those 22 tables are exactly the 22 classes that extend AuditableEntity. The narrowing is runtime-INERT because application.properties:21 sets spring.jpa.hibernate.ddl-auto=none, so Hibernate generates and validates no DDL from the annotation; it is a declaration fix, not a schema fix. The true upper bound on the value is 100 regardless: AuditEntityListener writes SecurityContextHelper.getCurrentUsername(), which is Authentication.getName(), which JwtAuthenticationFilter.java:79/99/102 sets from the JWT subject only after userRepository.findByUsername(...) resolves it (JwtAuthenticationFilter.java:85), so the value is always either a real SEC_USER.username — VARCHAR(100) NOT NULL at V16__sec_schema.sql:44 — or the literal 'system' fallback. Platform-wide, affecting CU/NOTIF/FILE as much as FIN, so still not FIN's to settle; written up for a human at governance/project-artifacts/platform-audit-widths-and-error-localization.md. === UPDATE 2026-09-12 (FINAL governance-state session; every figure below re-measured by this session, none copied) — part (d) IS NOW CLOSED, and this is the migration that closed it: src/main/resources/db/migration/V29__fin_comment_corrections.sql. V29 issues two COMMENT statements and nothing else — it changes no structure and no data. The first, read in the file today, is `COMMENT ON TABLE FIN_ACCOUNT IS 'ENT-FIN-001 Account — PRIVATE; [DBF-FIN-001..013, DBF-FIN-147]';`, which is the text db-script-fin.md:447 states, against V22__fin_schema.sql:240's `[DBF-FIN-001..013]` (both re-read at those line numbers today). The second replaces V22:278's FIN_JOURNAL_ENTRY.doc_no comment, which said 'platform numbering engine' — a component that does not exist in this repository — with one naming com.erp.fin.numbering.JournalDocNoGenerator and the JV-{fiscalYearCode}-{NNNNNN} format. V22 itself was NOT edited and must never be: this is a forward correction, the only legal repair for an applied, Flyway-checksummed migration. STATED PLAINLY SO THIS IS NOT OVER-READ: V29 IS WRITTEN BUT NOT YET APPLIED TO ANY DATABASE. It is an untracked new file in the working tree and the highest-numbered migration in src/main/resources/db/migration/ (verified by listing that directory today). Until a Flyway run executes it, the LIVE table comment still omits DBF-FIN-147 and the live doc_no comment still names a nonexistent engine. The ALIGN self-check's SCHEMA COMMENT line is closed on exactly this basis and says so on its own line. ONE DEFECT IN THE SAME FAMILY WAS DELIBERATELY LEFT OPEN AND IS NOW RECORDED SEPARATELY: V22__fin_schema.sql:4's header reads '146 DBF-IDs', stale since DBF-FIN-147; V29 did not touch it because it is a FILE HEADER COMMENT, not a COMMENT ON statement, so it was never written into the database and no live object misreports anything — and a forward migration cannot rewrite an earlier file's header in any case. See the new V22 header-count entry appended below. THE AuditableEntity RESIDUAL IS UNCHANGED and still platform-wide: re-read today at AuditableEntity.java:26 and :32, both `length = 100`; the physical split re-counted from scratch by grepping '(created_by|updated_by) +VARCHAR' across src/main/resources/db/migration/ and discarding the eight V2 tables V14 drops — 16 LIVE tables at VARCHAR(100) (SEC 5 in V16, MDL 2 in V18, FIN 9 in V22) and 6 at VARCHAR(255) (V1 CU_APP_CONFIGURATION, V6 NOTIF_TEMPLATE/NOTIF_CHANNEL_CONFIG/NOTIF_LOG, V8 FILE_CATEGORY/FILE_DOCUMENT), 22 in total, exactly the 22 classes `grep -rl \"extends AuditableEntity\" src/main/java/com/erp/` returns. No migration was written for it and none is proposed here. The 147-DBF and 16-XM-column counts on this line were both re-measured today (147 unique DBF-FIN ids in the manifest; 16 XM-FIN-001 COMMENT ON COLUMN lines in V22, out of 18 total XM-FIN-001 mentions — the other two are prose at V22:6 and :482) and both still hold.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "XM registry — FIN's consumption of com.erp.sec.crossmodule.SecUserDirectoryApi carries no XM id",
      "detail": "The ALIGN block's CROSS-MODULE line reads '1 XM from db-script, 1 placed (XM-FIN-001), 0 mismatched'. That was true when written, but SEC-BE landed afterwards and added a SECOND cross-module consumption: FinSeparationOfDutiesService injects com.erp.sec.crossmodule.SecUserDirectoryApi and calls findUserIdsHoldingPermission twice (service/FinSeparationOfDutiesService.java:70, 97-100) to resolve RULE-FIN-015. The boundary itself is correct — FIN's only non-FIN imports anywhere in src/main/java/com/erp/fin are com.erp.mdl.crossmodule.{MdlLookupApi, LookupOptionView} and com.erp.sec.crossmodule.SecUserDirectoryApi, and CrossModuleBoundaryArchTest passes — but the SEC read is registered in no XM table, has no id, no ACTIVE/DEFERRED status, and no fallback-if-absent contract, so nothing upstream records that FIN depends on SEC's user directory. XM-FIN-001's own scope (SOFT-READ of MDL_LOOKUP_VALUE) does not cover it.",
      "resolution": "RESOLVED — registered as XM-FIN-002, no code change. Re-verified first: FinSeparationOfDutiesService injects com.erp.sec.crossmodule.SecUserDirectoryApi and calls findUserIdsHoldingPermission twice in resolveFacts(), for PERM_FIN_PERIODS_CLOSE_APPROVE and PERM_FIN_JOURNAL_ENTRIES_CREATE; the call is in-process Spring interface injection, never HTTP; a failed read is caught and re-raised as FIN-403-SOD-VIOLATION, so the close is refused rather than approved on an unverified fact; FIN's only non-FIN imports remain MdlLookupApi/LookupOptionView and SecUserDirectoryApi, and CrossModuleBoundaryArchTest still passes. Placed as XM-FIN-002 (READ, ACTIVE, traces REQ-FIN-037/038) in every place an XM row belongs: db-script-fin.md §2 XM REGISTER and §7 XM id definitions; registry-db-fin.md's XM index and 'Last XM'; the XM id definitions list, the XM STATUS line, the extraction block and the CROSS-MODULE/Coverage lines in BOTH _SECTIONS.md and P3_1; a full XM:XM-FIN-002 block in INT-C (with its own Target/Interface/Contract/Blocks lines, matching XM-FIN-001's shape and HTML markers); and a status row in INT-R. NOT placed in the DB Alignment Manifest: that manifest's XM column is per-column, and XM-FIN-002 binds no FIN column — it is a service-to-service read — so a row there would be meaningless; this is stated explicitly in the manifest preamble rather than left silent. JUDGEMENT vs ADR-FIN-001: ADR-FIN-001 is unchanged and still correct. What it excludes from XM is the AMBIENT platform integration with SEC — the principal on every request, the @PreAuthorize gate, and FIN's own self-registration of its module/screens/actions — which every module has identically and which no FIN code calls. XM-FIN-002 is categorically different on three counts: (1) it is FIN's own business logic, not the platform's, reading SEC's data ABOUT OTHER USERS as an input to a named FIN rule (RULE-FIN-015) rather than authorizing the current caller; (2) it is a named FIN service calling a named method on SEC's published crossmodule interface — structurally identical to XM-FIN-001's MdlLookupApi call, which IS an XM row; (3) it has a defined, FIN-owned fallback contract when the target is absent or fails (FIN-403-SOD-VIOLATION), which ambient authorization by definition does not. Leaving it unregistered would hide a real, code-level dependency on SEC's read model behind an ADR written about something else. Both the ADR row in db-script-fin.md §4 and INT-C's closing paragraph now state the distinction explicitly so the two are never conflated again. === SUPERSEDED 2026-09-12 (final governance-state pass) — XM-FIN-002 IS RETIRED. This entry is kept in full, unedited above, because it records a decision that was correct when taken; it is closed here, not rewritten. The dependency it registered no longer exists in the code. FinSeparationOfDutiesService — the sole injector of com.erp.sec.crossmodule.SecUserDirectoryApi anywhere in FIN — was deleted on 2026-09-12 by a recorded human decision (see the SEC-BE grant-owner entry above for the grounds). Re-measured today rather than inferred: `grep -rn SecUserDirectoryApi src/main/java/com/erp/fin/` returns ZERO hits, and the COMPLETE set of non-com.erp.fin, non-com.erp.common imports anywhere under src/main/java/com/erp/fin/ is now exactly two lines — `import com.erp.mdl.crossmodule.LookupOptionView;` and `import com.erp.mdl.crossmodule.MdlLookupApi;`. FIN therefore declares EXACTLY ONE XM: XM-FIN-001 (SOFT-READ -> MDL_LOOKUP_VALUE), ACTIVE. RETIRED, NOT DELETED, and the distinction is deliberate: the id XM-FIN-002 is kept, is never reused and is never renumbered; it survives as a struck historical block in INT-C and INT-R and as a struck row wherever an XM register lists it; it binds no DB Alignment Manifest row and never did, because it was a service-to-service read rather than a column. The ALIGN self-check's CROSS-MODULE line now reads '1 XM, 1 placed, 0 mismatched' and says explicitly that the previous '2 XM ... both ACTIVE' is superseded. Two consequences recorded elsewhere rather than buried here: the error code FIN-403-SOD-VIOLATION survives as a live constant with no throw site, and SEC's findUserIdsHoldingPermission chain lost its only production caller — each has its own api_doc_gaps entry below. The ADR-FIN-001 judgement recorded above is NOT withdrawn: it remains the correct analysis of why that read WAS an XM row while it existed, and any future FIN read of another module's crossmodule interface must be registered the same way.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "POST /api/v1/fin/fiscal-years/{id}/year-end-close (API-FIN-027) — precondition ordering, rerun guard, and an empty closing entry",
      "detail": "Three smaller defects in the year-end close body, all below the 403 that currently blocks the endpoint entirely. (a) ORDERING CONTRADICTS THE DOCUMENTED POSTURE: FiscalYearService.yearEndClose's own javadoc states both preconditions are fail-fast — RULE-FIN-015 (403) and the all-periods-Hard-Closed check (409) — but the body (:153-166) runs findOrThrow, then successorOf, then retainedEarningsAccount BEFORE resolving the SoD facts and before any period check. A deployment missing the successor year or the Retained Earnings marker therefore answers FIN-404-YEAR or FIN-404-ACCOUNT where the plan says it should answer 403 or 409, which will make test assertions on this endpoint wrong whichever way they are written. (b) NO RERUN GUARD: nothing checks fiscalYear.statusCode != CLOSED before starting; only the per-period assertHardClosedForYearEnd (FiscalPeriodDomain:146-151) incidentally blocks a second run, because the first run set every period to YEAR_END_CLOSE. The guard is a side effect, not a stated rule. (c) EMPTY CLOSING ENTRY: if no result account carries a non-zero balance, closingLines (:205-230) returns an empty list and buildValidateAndPost posts an entry with ZERO lines — checkBalanced treats 0 = 0 as balanced (JournalEntryDomain:156-174). A zero-line posted journal entry is meaningless in a ledger.",
      "resolution": "RESOLVED for (a) and (b); (c) still needs the one-line human call. (a) ORDERING: FiscalYearService.yearEndClose now runs the SoD read (RULE-FIN-015, 403) first, then the year's own re-run guard, then the all-periods-Hard-Closed check (409), and only then resolves successorOf(...) and retainedEarningsAccount(). A deployment missing the successor year or the Retained Earnings marker therefore answers 403/409 as the javadoc and the plan both promise, instead of FIN-404-YEAR / FIN-404-ACCOUNT — test assertions on this endpoint are now writable either way. (b) RERUN GUARD: a new FiscalYearDomain (the entity's only Domain object; A.0.1 is unconditional and the guard's single fact is FiscalYear.statusCode, so DATA-DOM-MASTER's 'none scoped alone' no longer holds — its block is amended accordingly) carries assertCanYearEndClose(), which rejects a run against an already-CLOSED year with the catalog's EXISTING FIN-409-INVALID-TRANSITION row rather than a new code; it sits before the period check so a re-run is told the year is closed instead of the misleading FIN-409-PERIODS-NOT-CLOSED the first run's own YEAR_END_CLOSE transitions would raise. (c) EMPTY CLOSING ENTRY: left exactly as delivered and NOT guessed. REQ-FIN-036 and AC-FIN-036 require 'a balanced closing entry and a balanced opening entry', and POL-FIN-010 only requires result accounts to stand at zero afterwards — which a year with no result movement already satisfies. Nothing in P0/P1/P2/P3_1 chooses between posting a zero-line entry (today's behaviour, consuming a docNo), posting nothing (which breaks API-FIN-027's response contract, since it declares both entries) and refusing the close (which would block a legitimate dormant year). QUESTION FOR A HUMAN: when a fiscal year closes with no result account carrying a non-zero balance — and, symmetrically, no balance-sheet account carrying one — should API-FIN-027 post the zero-line entry, omit that entry from the response, or refuse the close? === UPDATE 2026-09-12 (governance-state alignment session) — part (c) is now RESOLVED; (a) and (b) stand as recorded above. DECISION: KEEP TODAY'S BEHAVIOUR — a dormant year-end close POSTS the zero-line CLOSING (and OPENING) entry, consuming a docNo. It was reviewed deliberately and kept, not left by default. Reasons, each checked against the code and the artifacts rather than asserted: posting it satisfies AC-FIN-036 LITERALLY, because JournalEntryDomain.checkBalanced sums debits and credits and returns empty when they compare equal, so 0 = 0 passes and a lineless entry is genuinely 'individually balanced'; OMITTING the entry would make AC-FIN-036's 'posts a closing entry' false and would also break API-FIN-027's declared response shape, which carries both entries; and REFUSING the close would invent a rule no artifact states, blocking a legitimate dormant year. NO CODE CHANGED — this is a decision recorded against behaviour that already shipped. It is now documented in the artifact rather than only here: see the DORMANT YEAR block at governance/modules/FIN/packages/backend-execution/SVC-API/SVC-API-INT.md:195-210, inside the <!-- API:API-FIN-027 --> block, which states the mechanism (FiscalYearService.closingLines contributes no line when every result account nets to zero and the Retained Earnings absorbing line is added only for a non-zero resultTotal; openingLines symmetrically), the docNo consumption (JournalPostingService.buildValidateAndPost allocates from the locked fiscal-year series before validating and writes unconditionally), the decision and its three-way rationale, and the containment fact that this shape is reachable ONLY from the internal year-end path because JournalEntryCreateRequest.lines carries @NotEmpty, so API-FIN-019 rejects a lineless entry at the DTO boundary. The question posed above is closed; do not re-open it as an unanswered gap.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "Error Catalog row FIN-403-FORBIDDEN; SEC-BE matrix FIN_ACCOUNTS/DELETE cell; execution-state api_docs_path",
      "detail": "Three small alignment defects found while re-checking the API and SECURITY self-check lines. (a) FIN-403-FORBIDDEN NEVER REACHES THE WIRE: the catalog registers it with ar+en messages and SEC-BE.md:36-37 asserts it 'maps through the same LocalizedException envelope as every other module'. It does not. GlobalExceptionHandler.handleAccessDenied (common/web/GlobalExceptionHandler.java:89-96) builds an ApiError with the hardcoded code 'ACCESS_DENIED' and a hardcoded English message, bypassing MessageSource entirely; FIN-403-FORBIDDEN appears in no FinErrorCodes constant and in neither i18n bundle. Every 403 a FIN caller receives therefore carries code ACCESS_DENIED, unlocalized — the Arabic client gets English. This is platform-wide (it affects SEC, MDL, CU, NOTIF, FILE identically), which is why no module-level sub caught it. (b) MATRIX CELL WITH NO PERMISSION: SEC-BE.md:8 marks FIN_ACCOUNTS/DELETE with '✓ deactivate (API-FIN-004)', but no PERM_FIN_ACCOUNTS_DELETE constant or V24 row exists and AccountService.deactivate (:140) is gated with PERM_FIN_ACCOUNTS_UPDATE. V24:49-54 rationalises the modelling, but the matrix still reads as a DELETE permission — the mirror image of the already-recorded FIN_PERIODS/VIEW cell. (c) DANGLING api_docs_path: execution-state.json declares api_docs_path 'governance/modules/FIN/api-docs/'; that directory does not exist and no api-docs-fin.md was produced. DOC.md deliberately deferred it ('the frontend stage binds to the real api-docs-fin.md published after implementation'), so the path points at nothing after the whole build. Everything else on those two self-check lines verified clean: 32 endpoints in the controllers exactly matching API-FIN-001..032, 25 PERM_FIN_* constants each matching a V24 row character-for-character and each used by a @PreAuthorize, 31 FinErrorCodes constants matching both i18n bundles exactly with no orphans in either direction, and no FIN service public method left ungated.",
      "resolution": "RESOLVED for (b) and (c); (a) ANNOTATED AND LEFT OPEN as a platform decision. (a) FIN-403-FORBIDDEN — confirmed exactly as described: GlobalExceptionHandler.handleAccessDenied hardcodes the code ACCESS_DENIED and an English message, bypassing MessageSource, and FIN-403-FORBIDDEN is in no FinErrorCodes constant (36 constants verified, matching both i18n bundles 36/36 with no orphan in either direction) and in neither bundle. Not fixed here — routing AccessDeniedException through LocalizedException changes every module's 403 body and needs a platform decision. Instead the Error Catalog row is now annotated in both copies as rendered on the wire as the platform ACCESS_DENIED envelope, not as a FIN code, and SEC-BE.md's 'Forbidden responses' paragraph — which asserted the opposite — was rewritten to say so; the ALIGN self-check carries it as an open ERROR ENVELOPE finding. (b) FIN_ACCOUNTS / DELETE — the ✓ was SPURIOUS and has been corrected, not left to a human: this one is derivable without a spec call, unlike FIN_PERIODS/VIEW. Grounds: AccountService.deactivate is gated on PERM_FIN_ACCOUNTS_UPDATE, the endpoint is PUT /{id}/deactivate, no PERM_FIN_ACCOUNTS_DELETE constant or V24 row exists, FIN publishes no DELETE endpoint at all, and V24:49-54 already rationalises the modelling. The ✓ moved from DELETE to UPDATE in SEC-BE.md (and the P3_1 mirror), with a new paragraph stating that the DELETE column is deliberately empty for every FIN screen; srs-fin.md's B4 Access lines and the Access summary table were corrected the same way (FIN_ACCOUNTS, FIN_RULES, FIN_RECURRING_TEMPLATES, FIN_ALLOCATION_RULES all claimed DELETE). While doing so, a further defect was found and fixed: srs-fin.md gave FIN_DIMENSIONS 'UPDATE (deactivate only)', but no dimension update or deactivate endpoint exists and no PERM_FIN_DIMENSIONS_UPDATE is registered — the B1 'deactivate' operation is simply not delivered in v1, which the SRS now says. (c) api_docs_path — the directory does not exist and no api-docs-fin.md was produced; DOC.md deliberately deferred it. Generating one was out of this pass's scope (documentation reconciliation, not artifact generation), so api_docs_path is now the empty string rather than a path to nothing. Set it back when api-docs-fin.md is actually published. === UPDATE 2026-09-12 (governance-state alignment session) — part (a) is now PARTIALLY RESOLVED, in one half only; (b) and (c) stand as recorded above. RESOLVED HALF — the message is LOCALIZED. src/main/java/com/erp/common/web/GlobalExceptionHandler.java:90-96 was read after the change: handleAccessDenied now builds .code(CommonErrorCodes.ACCESS_DENIED) and .message(resolveMessage(CommonErrorCodes.ACCESS_DENIED, null)), routing through the same MessageSource helper at :109-115 that every LocalizedException already used; a new constant ACCESS_DENIED was added at com/erp/common/exception/CommonErrorCodes.java:11, and an ACCESS_DENIED key was added to BOTH bundles (src/main/resources/i18n/messages.properties:16 and messages_ar.properties:13). The English is BYTE-IDENTICAL to the string it replaced — 'You do not have permission to perform this operation' — so the English wire body is unchanged and only Arabic callers see a difference. The claim in the original detail above that the handler 'hardcodes ... an English message, bypassing MessageSource' is therefore no longer true and should not be re-quoted as current. STILL-OPEN HALF, deliberately — the WIRE CODE IS UNCHANGED. It is still ACCESS_DENIED and it is still NOT FIN-403-FORBIDDEN: re-grepped today, FIN-403-FORBIDDEN exists in no FinErrorCodes constant and in neither i18n bundle, so FIN's 403 remains the platform ACCESS_DENIED envelope rather than a FIN-coded LocalizedException. That half was NOT taken on: routing AccessDeniedException through LocalizedException as a FIN code would change every module's 403 body (SEC/MDL/CU/NOTIF/FILE identically) and is still an open platform decision, carried as the ERROR ENVELOPE finding in the ALIGN self-check. The error-code counts in (a) above were measured when there were 36; re-measured today there are 37 FinErrorCodes constants, 37 ^FIN- keys in messages.properties and 37 in messages_ar.properties, an exact 37/37/37 with no orphan in either direction — the extra one is FIN-404-DIMVALUE, added today for API-FIN-035. (c) api_docs_path is LEFT AS IT IS, the empty string: no api-docs were generated, because api-docs generation belongs to the test phase and the test phase has not run. === UPDATE 2026-09-12 (FINAL governance-state session; every figure below re-measured by this session, none copied) — part (a) IS NOW CLOSED. (b) and (c) stand exactly as recorded above, and (c) — api_docs_path — is still the empty string, deliberately left alone, because api-docs generation belongs to the test phase and the test phase has not run. WHAT CLOSED (a): a NEW file, src/main/java/com/erp/fin/security/FinForbiddenAdvisor.java, read in full today. It is a Spring `DefaultPointcutAdvisor` registered as an infrastructure-role @Component at `Ordered.HIGHEST_PRECEDENCE`; its `StaticMethodMatcherPointcut` matches any target class whose name starts with 'com.erp.fin.service.', and its MethodInterceptor catches `AccessDeniedException` from `invocation.proceed()` and re-throws `new LocalizedException(Status.FORBIDDEN, FinErrorCodes.FIN_403_FORBIDDEN)`. GlobalExceptionHandler's LocalizedException handler then renders it through MessageSource like any other FIN code. Supporting artifacts, all three re-grepped today and all three previously ABSENT: the constant FIN_403_FORBIDDEN at FinErrorCodes.java:384, the English key at messages.properties:172 ('You do not hold the Finance permission required for this operation') and the Arabic at messages_ar.properties:169. So a FIN 403 now carries code FIN-403-FORBIDDEN, localized — where the original detail above correctly reported that it 'NEVER REACHES THE WIRE'. That sentence is now HISTORY and must not be re-quoted as current. It is declared once for the module, never per endpoint and never as a feature-module @ControllerAdvice (gov-enforce-backend-contract CU.7 forbids one), mirroring com.erp.sec.security.SecForbiddenAdvisor, which exists in this repo today. THE BOUNDARY, NAMED RATHER THAN GLOSSED, because the closure is real but not unlimited: (1) only a denial raised INSIDE com.erp.fin.service.* is translated. A Spring Security FILTER-CHAIN denial never reaches an AOP proxy or GlobalExceptionHandler at all — it is written directly by SecSecurityErrorHandler, which SecurityConfig.java:59 wires as `.accessDeniedHandler(securityErrorHandler)` and which answers SEC-403-FORBIDDEN (SecSecurityErrorHandler.java:42). That path is UNREACHABLE for FIN today because SecurityConfig.java:56 authorizes with `.anyRequest().authenticated()` and declares no FIN authority rule — the only `.requestMatchers` are SEC's four public auth POSTs, /actuator/health and /info, springdoc's paths and /error — so every FIN permission denial is in fact a @PreAuthorize denial on a FIN service. Add one URL-level authority rule for a FIN path and that stops being true. (2) FIN-403-SOD-VIOLATION is UNAFFECTED and always was: it is thrown as a LocalizedException directly (FinSeparationOfDutiesService.java:109 and FiscalPeriodDomain.java:112), never as an AccessDeniedException, so the advisor never sees it and cannot overwrite it. (3) The platform-wide AccessDeniedException path still answers CommonErrorCodes.ACCESS_DENIED for any module that has no advisor of its own; that is unchanged and is not a FIN finding. ERROR-CODE COUNTS, re-measured today: 38 FinErrorCodes constants, 38 '^FIN-' keys in messages.properties, 38 in messages_ar.properties — an exact 38/38/38 diffed pairwise with zero orphans in either direction. The previous update recorded 37/37/37; the 38th is FIN-403-FORBIDDEN itself. The Error Catalog holds 39 code rows, so exactly two are deliberately not constants: FIN-503 (struck) and FIN-500 (infrastructure fallthrough). === UPDATE 2026-09-12 (final governance-state pass) — part (a) was RESOLVED earlier the same day and its closing line references have since MOVED; both facts are recorded here so neither is re-derived wrongly. FIN-403-FORBIDDEN now reaches the wire as itself: src/main/java/com/erp/fin/security/FinForbiddenAdvisor.java (a DefaultPointcutAdvisor at Ordered.HIGHEST_PRECEDENCE whose pointcut matches com.erp.fin.service.*) catches AccessDeniedException and re-throws LocalizedException(Status.FORBIDDEN, FinErrorCodes.FIN_403_FORBIDDEN) at FinForbiddenAdvisor.java:54, which GlobalExceptionHandler renders through MessageSource. The constant and both bundle entries exist. THEIR LINE NUMBERS ARE NOT WHAT THE EARLIER NOTE RECORDED: re-grepped today, FIN_403_FORBIDDEN is at FinErrorCodes.java:400 (an earlier note said :384), messages.properties:173 (said :172) and messages_ar.properties:170 (said :169) — all three shifted when FIN-409-NOT-ACTIVE and FIN-422-INVALID-PERCENTAGE-VALUE were added. The platform-wide half is unchanged and still open: GlobalExceptionHandler's own AccessDeniedException handler answers CommonErrorCodes.ACCESS_DENIED (now localized through MessageSource) for any module without an advisor of its own, and whether every module should get one is still a platform question. ALSO CORRECTED HERE: this entry's part (a) text, and the ALIGN block's former ERROR ENVELOPE line, both cited FIN-403-SOD-VIOLATION as the one code the advisor cannot overwrite 'because it is thrown by hand at FinSeparationOfDutiesService.java:109'. That file no longer exists and the code has no throw site at all — see the dedicated SOD DEAD CODE entry below. Parts (b) and (c) are unaffected; api_docs_path is still the empty string and is not touched by this pass. Re-measured today for the record: 40 FIN error-code constants against 40 `^FIN-` keys in each bundle, an exact three-way set match, where the note inside part (a) above says 36/36 — that figure is historical.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "srs-fin.md §B / Access summary — SCR-REQ-FIN-002 'deactivate' and SCR-REQ-FIN-003 'delete line' are specified but not delivered",
      "detail": "Found during the ALIGN-BE reconciliation sweep, while correcting the §B5 endpoint tables to the shapes the controllers actually expose. Two screen requirements state operations that no endpoint implements, and this is a code-vs-requirement gap, not a stale document: (1) SCR-REQ-FIN-002 B1 lists 'deactivate' among the operations for BOTH Dimension and DimensionValue, and its B4 Access line read 'VIEW, CREATE, UPDATE (deactivate only, modeled as UPDATE)'. DimensionController exposes only create, create-value, search and search-values; DimensionService has no deactivate method; PermissionConstants declares PERM_FIN_DIMENSIONS_CREATE and PERM_FIN_DIMENSIONS_VIEW but no PERM_FIN_DIMENSIONS_UPDATE, and V24 seeds no such row. The isActiveFl columns DBF-FIN-018 and DBF-FIN-029 exist and default TRUE, so a dimension or dimension value can be created but never switched off through the API. (2) SCR-REQ-FIN-003 B1 lists 'delete' for RuleLine and its B4 read 'DELETE (deactivate rule / delete line)'. There is no rule-line delete endpoint, no event-rule deactivate endpoint, and no PERM_FIN_RULES_DELETE. The same pattern is benign for FIN_RECURRING_TEMPLATES and FIN_ALLOCATION_RULES, whose UPDATE permission is genuinely used (by the run endpoints), but for FIN_DIMENSIONS the screen has no update-class action at all. The srs-fin.md B4 and Access-summary rows have been corrected to describe what is built and to say the operation is not delivered in v1; the requirement text in B1 was deliberately NOT rewritten, because the SRS is asserting a requirement the code did not meet and rewriting it away would erase the gap.",
      "resolution": "OPEN — needs a human decision, not derivable. Either (i) accept that dimension/dimension-value deactivation and rule-line deletion are out of scope for v1, in which case SCR-REQ-FIN-002's and SCR-REQ-FIN-003's B1 Operations lines should be narrowed to match (a spec change), or (ii) implement them: a PUT /api/v1/fin/dimensions/{id}/deactivate and PUT /api/v1/fin/dimensions/values/{id}/deactivate gated by a new PERM_FIN_DIMENSIONS_UPDATE, plus the event-rule/rule-line equivalents, each needing a new API-FIN id, a new PermissionConstants constant and a forward SEC seed migration. No code, permission or migration was invented here. Note the same question does NOT arise for FIN_ACCOUNTS, which does deliver deactivate (API-FIN-004, PUT /{id}/deactivate, gated by PERM_FIN_ACCOUNTS_UPDATE). === UPDATE 2026-09-12 (governance-state alignment session) — now RESOLVED, by a SPLIT decision: neither option (i) nor option (ii) wholesale, but each of the four operations decided on its own merits. BUILT (two of them). (1) DIMENSION-VALUE deactivate — API-FIN-035, PUT /api/v1/fin/dimensions/values/{id}/deactivate at DimensionController.java:80 on base path /api/v1/fin/dimensions, delegating to DimensionValueService.deactivate, gated by the NEW PermissionConstants.PERM_FIN_DIMENSIONS_UPDATE (declared at PermissionConstants.java:97, consumed at DimensionValueService.java:100), raising the NEW error code FIN-404-DIMVALUE (FinErrorCodes.java:188, messages.properties:154, messages_ar.properties:151, raised at DimensionValueService.java:106), and backed by the NEW migration V28__fin_dimensions_update_action.sql, which both REGISTERS the FIN_DIMENSIONS/UPDATE SEC_ACTION_REG row and EXPLICITLY grants it to SYS_ADMIN — the grant is not optional because V25__fin_role_grants.sql grants FIN's rows via a SELECT over the registry and has already run, so it can never retroactively see a row inserted now (the exact failure V19 shipped for MDL and V21 had to repair). Reason it was built: the operation is meaningful, DBF-FIN-029's isActiveFl exists and defaults TRUE, and without it a dimension value could be created but never switched off. (2) EVENT-TYPE-RULE deactivate — API-FIN-034, PUT /api/v1/fin/event-rules/{id}/deactivate at EventTypeRuleController.java:66, gated by the PRE-EXISTING PERM_FIN_RULES_UPDATE (EventTypeRuleService.java:100) and raising the PRE-EXISTING FIN-404-RULE (FinErrorCodes.java:194), so it needed NO new constant, NO new error code and NO migration at all. DELIBERATELY NOT BUILT (the other two), and now recorded in srs-fin.md as v1 DECISIONS with their reasons rather than as gaps — so they are not open items and should not be re-raised as such. (3) A deactivate on the PARENT Dimension: srs-fin.md:1154 narrows SCR-REQ-FIN-002's B1 Operations to 'search, create (dimension — no deactivate, deliberate; see B4)', and srs-fin.md:1179 gives the reason — no REQ, AC or RULE asks for one; DBF-FIN-018's isActiveFl consequently stays unflippable through the API, which V28's own header states explicitly so the correction is not overstated. (4) A RULE-LINE delete: srs-fin.md:1212 and :1243 record it as a deliberate v1 exclusion because ENT-FIN-010 carries no active-flag column, and srs-fin.md:1578 restates it module-wide — FIN publishes no DELETE endpoint on any screen, verified today by grepping for @DeleteMapping across src/main/java/com/erp/fin/controller/, which returns zero hits. The requirement text was narrowed WITH its reason attached, not silently deleted.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "V24__fin_security_seed.sql:52-53 — a fabricated quotation attributed to srs-fin.md, inside an applied and immutable migration",
      "detail": "V24__fin_security_seed.sql:52-53 asserts, as grounds for seeding no PERM_FIN_*_DELETE rows, that srs-fin.md's B4 \"Actions:\" lines for FIN_DIMENSIONS, FIN_RULES, FIN_RECURRING_TEMPLATES and FIN_ALLOCATION_RULES `say \"DELETE (deactivate ..., modeled as UPDATE)\"`. That quotation does not exist. Measured: `grep -c 'DELETE (deactivate' governance/modules/FIN/P1/srs-fin.md` returns 0 and `grep -c 'modeled as UPDATE' governance/modules/FIN/P1/srs-fin.md` returns 0 — the string occurs nowhere in srs-fin.md in any spelling. What those four lines actually read, opened at srs-fin.md:1166, :1227, :1270 and :1296, is `Actions: VIEW, CREATE, UPDATE` (two of them with a parenthetical about \"run\"); none of the four mentions DELETE at all. The phrase's real home is MDL, where it is genuine: `(deactivate only, modeled as UPDATE — no hard-delete endpoint exists)` at governance/modules/MDL/packages/backend-execution/SEC-BE/SEC-BE.md:8 and governance/modules/MDL/P3_1/backend-execution-plan-mdl.md:509. So it was carried across modules by copy-from-a-neighbour rather than read out of FIN's own SRS. Note the CONCLUSION V24 draws from it — seed no DELETE rows for those screens — is independently correct and is not in dispute; only the evidence cited for it is fabricated.",
      "resolution": "OPEN, and NOT CORRECTABLE AT SOURCE — recorded here precisely so it stops propagating. V24__fin_security_seed.sql is an applied migration and Flyway checksums it, so the comment cannot be edited; a forward migration cannot amend a comment in an earlier file either. This entry and the DOC QUOTATION line in the ALIGN self-check (mirrored in _SECTIONS.md and P3_1/backend-execution-plan-fin.md) are the mutable record. Any future session that encounters V24:52-53 must treat the quoted string as NOT srs text and must re-read srs-fin.md:1166/1227/1270/1296 before restating it. The same phrasing already appears once more in this file, in the pre-existing detail of the SCR-REQ-FIN-002/003 entry ('VIEW, CREATE, UPDATE (deactivate only, modeled as UPDATE)'), which is a historical record of what an earlier session believed and is left untouched by rule — it is NOT evidence that the string exists in srs-fin.md. === UPDATE 2026-09-12 (FINAL governance-state session; every figure below re-measured by this session, none copied) — STILL OPEN, re-verified today, and STRUCTURALLY UNCLOSEABLE — nothing has changed and nothing can. V24__fin_security_seed.sql:52-53 was re-read at those exact lines and still asserts that srs-fin.md's B4 'Actions:' lines say \"DELETE (deactivate ..., modeled as UPDATE)\". Re-measured today with the same two commands: `grep -c 'DELETE (deactivate' governance/modules/FIN/P1/srs-fin.md` returns 0 and `grep -c 'modeled as UPDATE' governance/modules/FIN/P1/srs-fin.md` returns 0. Importantly, the SRS WAS EDITED today by another session (the Operations narrowing recorded in the separate B1-Operations entry), so this re-measurement is not a stale carry-over: the fabricated string is absent from the CURRENT srs-fin.md as well. The real Actions/Operations text now sits at srs-fin.md:1166, :1224, :1270 and :1354 and none of the four mentions DELETE. V24 is applied and Flyway-checksummed, so it can never be corrected at source, and a forward migration cannot amend a comment inside an earlier file — the correction channel that closed the V22 comment defect (V29) does not exist for file comments. This entry and the DOC QUOTATION line of the ALIGN self-check remain the only mutable record, and DOC QUOTATION is one of the six open findings in the refreshed block for exactly this reason. It must not be closed by any future session; it can only be inherited.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "Error Catalog rows FIN-404-YEAR and FIN-404-PERIOD — the API column under-lists the endpoints that raise each code",
      "detail": "Measured by grepping FinErrorCodes.FIN_404_YEAR and FinErrorCodes.FIN_404_PERIOD across src/main/java/com/erp/fin/ and reading every throw site. The Error Catalog row for FIN-404-YEAR (_SECTIONS.md:407 and its P3_1 mirror) names API-FIN-027, 030, 031 in its API column, but the code is also thrown at JournalEntryService.java:171 (createManual, behind API-FIN-019), JournalPostingService.java:152 (the shared posting path behind API-FIN-019/020/021) and FiscalYearService.java:364 (successorOf) and :371 (findOrThrow), behind API-FIN-023 and API-FIN-027. The row for FIN-404-PERIOD (_SECTIONS.md:405 and mirror) names API-FIN-024, 025, 026, 029, 031, but the code is also thrown at RecurringTemplateService.java:284 (template run, API-FIN-014), JournalEntryService.java:174 (API-FIN-019), JournalPostingService.java:208 and FiscalYearService.java:314 and :322 (year-end close, API-FIN-027). Consequence: a test author or an api-docs generator reading only the catalog would not know that API-FIN-014/019/020/021/023 can answer these 404s, and would not write assertions for them.",
      "resolution": "OPEN — recorded, deliberately NOT fixed by this session. The two catalog rows sit OUTSIDE the ALIGN self-check block, in the part of _SECTIONS.md and P3_1/backend-execution-plan-fin.md that three earlier sessions already aligned today; this session's scope was the self-check block, this file and one project-artifacts report, so it did not edit them. The fix is mechanical and low-risk when someone picks it up: extend both API columns to the endpoint lists above, applying the identical edit to BOTH mirrors (_SECTIONS.md and P3_1/backend-execution-plan-fin.md) and re-verifying byte-equality afterwards. Nothing in the CODE is wrong here — every throw site is correct and intentional; only the documentation under-reports them. === UPDATE 2026-09-12 (FINAL governance-state session; every figure below re-measured by this session, none copied) — NOW CLOSED — a later session made the mechanical fix this entry described, and this session re-verified the result against the code rather than accepting it as done. Both API columns were re-read today: the FIN-404-YEAR row now lists API-FIN-014, 017, 019, 020, 021, 027, 030, 031 and the FIN-404-PERIOD row lists API-FIN-014, 017, 019, 020, 024, 025, 026, 027, 029, 031, identically in _SECTIONS.md and in the P3_1 mirror. Re-grepping both constants across src/main/java/com/erp/fin/ today finds FIN_404_YEAR thrown at ReportService.java:329, FiscalYearService.java:364 (successorOf) and :371 (findOrThrow), JournalEntryService.java:171 and JournalPostingService.java:152; and FIN_404_PERIOD at RecurringTemplateService.java:284, ReportService.java:337, FiscalYearService.java:314 and :322, JournalEntryService.java:174, FiscalPeriodService.java:204 and JournalPostingService.java:208. Every one of those paths is now covered by the corrected columns. ONE CORRECTION TO THIS ENTRY'S OWN DETAIL, made because it was wrong rather than left standing: the detail above says FiscalYearService.java:371 (findOrThrow) sits 'behind API-FIN-023 and API-FIN-027'. It does not. findOrThrow has exactly one caller, FiscalYearService.java:168, which is the year-end close — API-FIN-027 only. API-FIN-023 (create fiscal year) never calls it and cannot raise FIN-404-YEAR that way, which is why the corrected catalog row correctly omits 023. Also measured for the record: 12 Error Catalog rows differ from HEAD in the working tree, not the 9 an earlier summary claimed — the three additional rows (FIN-404-TEMPLATE, FIN-404-ALLOCATION-RULE, FIN-403-FORBIDDEN) are the API-FIN-036/037 and FinForbiddenAdvisor deliveries landing after that count was taken. Report 404 behaviour re-read and correct: ReportService.assertFiscalYearExists (:326-330) resolves the REQUIRED fiscalYearId first on API-FIN-030/031, and trialBalance (:136-144) validates API-FIN-029's OPTIONAL periodId only `if (periodId != null)`.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "srs-fin.md B1 'Operations' lines — 'read' (and, on two screens, update/deactivate/line-CRUD) specified but not published by any controller",
      "detail": "Distinct from the now-closed SCR-REQ-FIN-002/003 entry, which was about deactivate/delete; this is about READ and the remaining template/rule operations. Verified by listing every @GetMapping/@PostMapping/@PutMapping/@PatchMapping/@DeleteMapping in each of the nine files under src/main/java/com/erp/fin/controller/ and comparing to each screen's B1 line. (1) srs-fin.md:1128 (SCR-REQ-FIN-001, Chart of accounts) lists 'search, create, read, update, deactivate'. AccountController publishes create (API-FIN-002), update (API-FIN-003), deactivate (API-FIN-004) and search (API-FIN-001) — there is NO GET /{id}. The screen's own B5 table at srs-fin.md:1145-1148 lists only those four rows, so the B1 'read' is the sole claim and nothing else in the SRS backs it. (2) srs-fin.md:1258 (SCR-REQ-FIN-004) lists 'search, create, read, update, deactivate (template); create, read, update, delete (line)'. RecurringTemplateController publishes exactly three endpoints: POST create (API-FIN-013), POST /{id}/run (API-FIN-014) and POST /search (API-FIN-012) — no read, no update, no deactivate, and no line endpoints at all (the lines travel inside the create body). (3) srs-fin.md:1283 (SCR-REQ-FIN-005) lists the same shape for rule and target. AllocationRuleController publishes exactly POST create (API-FIN-016), POST /{id}/run (API-FIN-017) and POST /search (API-FIN-015). (4) Flagged rather than asserted, same shape: srs-fin.md:1339 (SCR-REQ-FIN-007) lists 'create (year), search, read' — FiscalYearController publishes only POST create (API-FIN-023) and POST /{id}/year-end-close (API-FIN-027), and the search that now exists (API-FIN-033) is over fiscal PERIODS, not years. NOT affected, checked: SCR-REQ-FIN-006, whose 'read' is delivered by JournalEntryController's GET /{id} (API-FIN-022), and SCR-REQ-FIN-008 (srs-fin.md:1390), whose 'read' is the account-ledger report.",
      "resolution": "OPEN — needs a human call, one screen at a time, and NOT derivable from the artifacts. For each of (1)-(4) the choice is the same: either narrow the B1 Operations line to what v1 delivers (a spec change, and if taken it should carry its reason inline, the way srs-fin.md:1154/1179 and :1243/:1578 already record the dimension and rule-line exclusions), or schedule the endpoints — each of which would need a new API-FIN id, a controller method, service gating on an EXISTING screen VIEW permission (PERM_FIN_ACCOUNTS_VIEW, PERM_FIN_RECURRING_TEMPLATES_VIEW, PERM_FIN_ALLOCATION_RULES_VIEW and PERM_FIN_FISCAL_YEARS_VIEW are all already registered and granted, so a read endpoint needs no migration) and a test-plan case. Deliberately NOT actioned here: srs-fin.md is outside this session's scope and no endpoint, constant or migration was invented. Nothing is BROKEN — this is a requirement-vs-delivery divergence, not a defect in shipped code. === UPDATE 2026-09-12 (FINAL governance-state session; every figure below re-measured by this session, none copied) — NOW CLOSED — the human call this entry asked for was taken, and it was taken the way the entry recommended: NARROW THE SPEC, with the reason attached inline, rather than schedule endpoints. All six B1 Operations lines were re-read today at their current line numbers, which have shifted from those in the detail above. srs-fin.md:1128 (SCR-REQ-FIN-001) now reads 'search, create, update, deactivate. No by-id read — deliberate v1 exclusion, see the note under B5'. :1166 (SCR-REQ-FIN-002) reads 'search, create (dimension — no deactivate, deliberate; see B4); search, create, deactivate (value — API-FIN-035)'. :1224 (SCR-REQ-FIN-003) reads 'search, create, deactivate (rule — API-FIN-034); create (line). As built there is no update and no by-id read on either, and no line delete — see B4'. :1270 (SCR-REQ-FIN-004) and :1354 (SCR-REQ-FIN-005) now name exactly API-FIN-012/013/014/036 and API-FIN-015/016/017/037 — everything RecurringTemplateController and AllocationRuleController publish, confirmed today by listing every mapping in both files (four each). :1467 (SCR-REQ-FIN-007) reads 'create (year); search — PERIODS only (API-FIN-033); open, soft-close, hard-close (period); run year-end close. There is NO fiscal-YEAR search and no by-id read of either a year or a period'. SCR-REQ-FIN-006 and SCR-REQ-FIN-008 were never affected and are unchanged. TWO THINGS THIS CLOSURE DOES NOT COVER, both of which the narrowed SRS text itself now flags in bold and both of which are recorded as NEW api_doc_gaps entries appended below rather than folded into this closure: (1) deactivate is delivered for template and rule (API-FIN-036/037) but does NOT stop the run; (2) update is still not built for either aggregate. Closing this entry without separating those two would have been a false closure, which is why they are separate entries.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "common/web/GlobalExceptionHandler — INTERNAL_ERROR, VALIDATION_ERROR and DATA_INTEGRITY_VIOLATION still return hardcoded English (platform-wide)",
      "detail": "Read at src/main/java/com/erp/common/web/GlobalExceptionHandler.java:54-105. After today's AccessDeniedException change, exactly two handlers resolve through MessageSource: the LocalizedException handler (:32-46) and handleAccessDenied (:90-96). Three still build hardcoded English strings. (a) handleUnexpected (:99-105) sets .code(\"INTERNAL_ERROR\") and .message(\"An unexpected error occurred\") — yet BOTH bundles ALREADY carry an INTERNAL_ERROR entry that nothing resolves: messages.properties:15 reads 'An unexpected error occurred. Please try again later.' and messages_ar.properties:12 carries the Arabic. The two English strings DIFFER, so routing this code through resolveMessage would silently change the wire text for every existing English caller — the fix is not mechanical. (b) handleValidation (:54-68) sets .code(\"VALIDATION_ERROR\") and .message(\"Validation failed\"), and handleMalformedRequestBody (:70-78) sets the same code with 'The request body is malformed or does not match the expected structure' — two different English messages under one code. (c) handleDataIntegrity (:80-88) sets .code(\"DATA_INTEGRITY_VIOLATION\") and 'The request could not be completed because it violates a data constraint'. For (b) and (c) there is NO bundle entry in either language: grep '^VALIDATION_ERROR=' and grep '^DATA_INTEGRITY_VIOLATION=' return nothing in messages.properties and nothing in messages_ar.properties. Note also that CommonErrorCodes declares VALIDATION_ERROR (:9) and INTERNAL_ERROR (:10) as constants, but these three handlers use string literals rather than the constants. Affects every module identically — SEC, MDL, FIN, CU, NOTIF, FILE — so an Arabic caller receives English for any 400/409/500 that is not a LocalizedException.",
      "resolution": "OPEN — one half needs a HUMAN DECISION before any code can be written, the other half needs new bundle keys. The human decision is the INTERNAL_ERROR wording conflict: the hardcoded 'An unexpected error occurred' and the bundle's 'An unexpected error occurred. Please try again later.' are not the same string, so someone must choose which becomes the wire text before handleUnexpected is routed through resolveMessage — picking one silently would change observable output for existing English callers, which is exactly the trap today's AccessDeniedException change avoided (its English was byte-identical, so nothing English-facing moved). For VALIDATION_ERROR and DATA_INTEGRITY_VIOLATION there is no conflict, only absence: keys must be minted in both bundles first, and VALIDATION_ERROR needs a decision about whether its two distinct messages keep one code or get separate keys. Deliberately NOT actioned here: this is platform-level, not FIN's to settle, and this session edits state and self-check records only. Written up for a human at governance/project-artifacts/platform-audit-widths-and-error-localization.md. === UPDATE 2026-09-12 (FINAL governance-state session; every figure below re-measured by this session, none copied) — NOW PARTIALLY CLOSED — three of the three named handlers were localized; ONE RESIDUE, not previously the focus of this entry, remains open. Re-read at GlobalExceptionHandler.java:54-116 today. (a) INTERNAL_ERROR IS CLOSED, and the wording conflict this entry said 'needs a HUMAN DECISION' WAS DECIDED — in favour of the BUNDLE text. handleUnexpected (:108-116) now builds `.code(CommonErrorCodes.INTERNAL_ERROR)` and `.message(resolveMessage(CommonErrorCodes.INTERNAL_ERROR, null))`, so the wire now returns messages.properties:15 'An unexpected error occurred. Please try again later.' instead of the handler's former 'An unexpected error occurred'. STATED PLAINLY rather than glossed: ENGLISH 500 BODIES DID MOVE. That is option (i) of the two this entry laid out, not the byte-identical option (ii) that the ACCESS_DENIED change used — a deliberate divergence from that precedent, recorded here so nobody later reads the 500 text change as accidental drift. (c) DATA_INTEGRITY_VIOLATION IS CLOSED: handleDataIntegrity (:89-97) resolves through MessageSource, a DATA_INTEGRITY_VIOLATION constant was added at CommonErrorCodes.java:12, and the key now exists in both bundles (messages.properties:18, messages_ar.properties:15). (b) IS HALF CLOSED. handleValidation (:54-68) now resolves `CommonErrorCodes.VALIDATION_ERROR` through MessageSource and the key exists in both bundles (messages.properties:17 'Validation failed', messages_ar.properties:14). THE RESIDUE: handleMalformedRequestBody (:79-87) still hardcodes the English string 'The request body is malformed or does not match the expected structure', and its own javadoc at :70-78 now states why — it emits the SAME wire code as handleValidation (VALIDATION_ERROR) but a DIFFERENT sentence, and one bundle key cannot carry two messages. So an Arabic caller still receives English for a malformed request body, and ONLY for that. Closing it is a WIRE-CONTRACT change, not a localization change: either give that response its own code (e.g. MALFORMED_REQUEST_BODY) with its own pair of keys, or collapse the two sentences into one. The cosmetic note in the detail above is also resolved: all four handlers now use CommonErrorCodes constants (:9 VALIDATION_ERROR, :10 INTERNAL_ERROR, :11 ACCESS_DENIED, :12 DATA_INTEGRITY_VIOLATION) rather than string literals. Still platform-wide and still not FIN's to settle; governance/project-artifacts/platform-audit-widths-and-error-localization.md was refreshed today to this state.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "PUT /api/v1/fin/recurring-templates/{id}/deactivate (API-FIN-036) and PUT /api/v1/fin/allocation-rules/{id}/deactivate (API-FIN-037) — deactivation does NOT stop the run (API-FIN-014, API-FIN-017)",
      "detail": "A DEFECT IN SHIPPED BEHAVIOUR, not a documentation divergence, and the only one of the six open ALIGN findings that is. Verified on 2026-09-12 by reading both run paths in full and grepping both services. Neither RecurringTemplateService.run (src/main/java/com/erp/fin/service/RecurringTemplateService.java:192, behind API-FIN-014) nor AllocationRuleService.run (src/main/java/com/erp/fin/service/AllocationRuleService.java:170, behind API-FIN-017) reads isActiveFl anywhere. In RecurringTemplateService the only isActiveFl occurrences are the search allow-list at :81 and two javadoc notes at :335 and :343; in AllocationRuleService, the search allow-list at :73 and javadoc at :215-223. AllocationRuleDomain.isActive() (src/main/java/com/erp/fin/domain/AllocationRuleDomain.java:180) still has ZERO call sites — its only other mentions anywhere under src/main/java/com/erp/fin/ are two {@link} javadoc references inside AllocationRuleService itself. CONSEQUENCE: a template or allocation rule deactivated through API-FIN-036/037 STILL RUNS and STILL POSTS a journal entry. The flag is settable and searchable but enforces nothing. Nothing about this is hidden — both services' javadoc says so outright, srs-fin.md:1270 and :1354 carry it as a bold OPEN DEFECT, and backend-test-plan-fin.md's TC-FIN-106 and TC-FIN-107 deliberately PIN the current behaviour rather than assert a requirement — but nothing prevents it either, and the test phase has not run, so even those two TCs are unexecuted.",
      "resolution": "OPEN — AN EXPRESS DECISION FOR A HUMAN, AND NO EXECUTION PHASE MAY CLOSE IT UNILATERALLY. Gating the run requires a new error code meaning 'this template/rule exists but is inactive', and NO RULE-FIN-* states that behaviour: RULE-FIN-006/007/008/009 govern the CONTENT of what is posted (balance, postable account, open period, valid dimension), not the template's or rule's own lifecycle, and no REQ-FIN-* or AC-FIN-* specifies it either. So the code itself, its HTTP status, its ar+en bundle messages and its Error Catalog row would all have to be INVENTED — which is writing new requirement, not implementing existing requirement. The alternatives a human must choose between are genuinely different products: (i) gate the run and 409 on an inactive template/rule (needs the new code, plus test cases, plus a decision on whether an in-flight schedule is cancelled); (ii) keep the run ungated and re-document deactivate as 'hide from the list' rather than 'stop', narrowing srs-fin.md accordingly; (iii) remove isActiveFl from the search allow-list too, on the grounds that a flag enforcing nothing should not be a filter. DELIBERATELY NOT ACTIONED by this session, which edits state and self-check records only and invents no endpoint, code or migration. Carried as the DEACTIVATE RUN line of the ALIGN self-check in _SECTIONS.md and its P3_1 mirror. === CLOSED 2026-09-12 (final governance-state pass) — RESOLVED BY A RECORDED HUMAN DECISION, taken as option (i) of the three this entry laid out. The decision this entry said only a human could make WAS made, and the gate was built. Deactivation now stops the run. THE ARTIFACTS, each opened and read before this clause was written, because a closure that does not name its artifact is not a closure: (a) the new error code — `public static final String FIN_409_NOT_ACTIVE = \"FIN-409-NOT-ACTIVE\";` at src/main/java/com/erp/fin/exception/FinErrorCodes.java:441 — with a key in BOTH bundles (confirmed by a three-way set diff: 40 `\"FIN-*\"` literals in FinErrorCodes.java, 40 `^FIN-` keys in messages.properties, 40 in messages_ar.properties, no orphan in any direction); (b) RecurringTemplateDomain.assertCanRun() at src/main/java/com/erp/fin/domain/RecurringTemplateDomain.java:71-76, throwing `new LocalizedException(Status.CONFLICT, FinErrorCodes.FIN_409_NOT_ACTIVE, recurringTemplatePk)` when `!active` — RecurringTemplateDomain is a NEW Domain companion, taking FIN from 7 Domain classes to 8; (c) AllocationRuleDomain.assertCanRun() at src/main/java/com/erp/fin/domain/AllocationRuleDomain.java:191-196, same shape with allocationRulePk — which also gives AllocationRuleDomain.isActive() the call path it had lacked, retiring the ZERO-call-site A.2.9 condition this entry recorded against it; (d) the two call sites that make the guards live, RecurringTemplateService.java:206 (API-FIN-014) and AllocationRuleService.java:182 (API-FIN-017), both re-read today; (e) Status.CONFLICT, which maps to HTTP 409; and (f) backend-test-plan-fin.md's TC-FIN-106 and TC-FIN-107, FLIPPED from pinning the defect to asserting the refusal — now titled 'a deactivated recurring template is REFUSED at run time (FIN-409-NOT-ACTIVE)' (:1449) and the allocation-rule equivalent (:1483). srs-fin.md:1281 and :1369 were rewritten to match and now read that deactivate 'DOES stop the template/rule from running — that defect is CLOSED'. WHAT THIS CLOSURE RESTS ON, STATED PLAINLY RATHER THAN GLOSSED: NO RULE-FIN-* STATES THIS GATE, and none was invented to pretend otherwise. RULE-FIN-006/007/008/009 govern the CONTENT of what is posted, not a template's or rule's own lifecycle, and no REQ-FIN-* or AC-FIN-* specifies it. The gate rests ENTIRELY on a recorded human decision of 2026-09-12 — which is precisely the authority this entry said was required before any execution phase could close it, and it was obtained rather than assumed. The Error Catalog row at _SECTIONS.md:393 states this in its own RULE column, which reads '(no RULE — recorded human decision, 2026-09-12)', and the test plan's traceability rows for TC-FIN-106/107 repeat it, so the provenance is carried in the artifacts and not only here. OPTION (iii) — removing isActiveFl from the search allow-list — was NOT taken and was not part of the decision; the flag remains searchable. Carried as the DEACTIVATE RUN line of the ALIGN self-check in _SECTIONS.md and its P3_1 mirror, which now reads CLOSED.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "RecurringTemplateController and AllocationRuleController — neither a recurring template (ENT-FIN-011/012) nor an allocation rule (ENT-FIN-013/014) can be corrected after creation; no update endpoint exists",
      "detail": "Measured on 2026-09-12 by listing every @GetMapping/@PostMapping/@PutMapping/@PatchMapping/@DeleteMapping in both controllers. RecurringTemplateController publishes exactly four: POST create (:51, API-FIN-013), POST /{id}/run (:62, API-FIN-014), PUT /{id}/deactivate (:68, API-FIN-036) and POST /search (:76, API-FIN-012). AllocationRuleController publishes exactly four: POST create (:52, API-FIN-016), POST /{id}/run (:63, API-FIN-017), PUT /{id}/deactivate (:69, API-FIN-037) and POST /search (:76, API-FIN-015). There is no PUT /{id} on either, and no line- or target-level endpoint of any kind — lines and targets travel inside the create body only — and the matching services expose no update method (RecurringTemplateService's public methods are create/run/deactivate/search; AllocationRuleService's are create/run/deactivate/search). PRACTICAL CONSEQUENCE: a typo in a template line's account or amount, or a wrong allocation percentage, can only be worked around by creating a replacement and deactivating the original — and per the companion DEACTIVATE RUN entry, deactivating the original does not actually stop it running. srs-fin.md:1270 and :1354 now state this openly, each saying update 'is still NOT built and remains the open half of the same gap'.",
      "resolution": "OPEN — recorded as a KNOWN GAP WITH NO DERIVABLE REQUIREMENT. No REQ-FIN-*, AC-FIN-* or RULE-FIN-* specifies update semantics for either aggregate, and the unanswered questions are substantive rather than mechanical: what happens to an in-flight schedule when a template's nextRunDate or cadence changes; whether entries already posted from the old definition are re-derived, left alone, or flagged; whether an allocation rule's target set may shrink after it has run, and what that means for the remainder line RULE-FIN-003/010 govern; whether an update is barred once the rule has posted at all, by analogy with RULE-FIN-016's post-lock on journal entries. None of that is derivable from the artifacts, so no session can write it without inventing requirement. A human must either write the requirement or record the exclusion explicitly — the way the parent-Dimension deactivate and the rule-line delete were recorded as reasoned v1 exclusions with their grounds inline at srs-fin.md, which is the precedent to follow. DELIBERATELY NOT ACTIONED here. Carried as the TEMPLATE/RULE UPD line of the ALIGN self-check.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "POST /api/v1/fin/journal-entries (API-FIN-019) — the SVC-API-CRUD.md block and its P3_1 mirror are NOT identical; the Numbering paragraph exists in one copy only",
      "detail": "PRE-EXISTING and unresolved; surfaced on 2026-09-12 while verifying that the ALIGN self-check's two copies are byte-equal. Measured by extracting governance/modules/FIN/packages/backend-execution/SVC-API/SVC-API-CRUD.md:180-222 (43 lines, between the API:API-FIN-019:START and :END markers) and governance/modules/FIN/P3_1/backend-execution-plan-fin.md:977-1009 (33 lines, same markers) and diffing them: 11 lines differ. A 10-line 'Numbering:' paragraph is present ONLY in SVC-API-CRUD.md — it specifies docNo = JV-{fiscalYearCode}-{NNNNNN}, the literal JV- prefix, the fiscal year's code (DBF-FIN-066, VARCHAR(10)), the zero-padded 6-digit counter starting at 000001, the worst-case 20 characters inside doc_no VARCHAR(30), the counter scoped per fiscalYearId and never segmented by journalTypeCode, UQ_FIN_JOURNAL_ENTRY_YEAR_DOCNO as the database backing, the FIN-local generator in com.erp.fin and the explicit statement that no platform numbering engine exists in this repo, and that docNo is assigned once on create and immutable thereafter. The Orchestration line differs correspondingly: the package copy reads 'generate docNo (FIN-local generator)' where P3_1 inlines it as 'generate docNo (FIN-local generator, `JV-{fiscalYearCode}-{NNNNNN}`)'. No fact contradicts the other — P3_1 carries LESS detail, not different detail — so nothing downstream is presently wrong. THE RISK is specific: a future session reading only P3_1 has no statement that the platform numbering engine does not exist, and could re-derive or re-invent the numbering scheme. That is exactly the failure mode the BINDING line and V29's second statement exist to prevent.",
      "resolution": "OPEN — deliberately NOT fixed by this session, which is scoped to the ALIGN self-check block, this file and one project-artifacts report; the API-FIN-019 blocks sit outside all three. The fix is mechanical and low-risk when someone authorized picks it up: copy the 10-line Numbering paragraph from SVC-API-CRUD.md:199-208 into the P3_1 block, re-align the Orchestration line to the package copy's wording, then re-verify byte-equality of the whole marker-delimited block in both files. NO OTHER API BLOCK WAS SURVEYED for the same defect — this one was found incidentally, so the correct next step is a sweep of every API:*:START..END pair across both files, not just this one repair. Recorded here so the sweep has a starting point. Carried as the MIRROR DIVERGENCE line of the ALIGN self-check, which also records that the self-check block ITSELF is byte-identical across both files, verified by md5 before and after today's revision.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "V22__fin_schema.sql:4 — the migration's header comment still reads '146 DBF-IDs', stale since DBF-FIN-147; deliberately NOT corrected by V29",
      "detail": "Read at src/main/resources/db/migration/V22__fin_schema.sql:4 on 2026-09-12, verbatim: '-- Target: POSTGRESQL_16 | 14 tables, 14 sequences | 146 DBF-IDs | 1 XM (SOFT-READ -> MDL)'. The count has been stale since V23__fin_account_retained_earnings_flag.sql added is_retained_earnings_fl as DBF-FIN-147. The current count is 147, re-measured today as 147 unique DBF-FIN-nnn ids in the DB Alignment Manifest. The same header's '1 XM' is likewise historical, since XM-FIN-002 (the SEC SecUserDirectoryApi read) was registered later at ALIGN-BE — though '16 XM-FIN-001 COMMENT ON COLUMN lines' was re-counted in V22 today and is unchanged and correct. WHY THIS IS LOWER SEVERITY THAN THE TWO DEFECTS V29 DID FIX: line 4 is a FILE HEADER COMMENT, not a COMMENT ON statement. It was never written into the database, so no live catalog object misreports anything, and no query, tool or api-docs generator reads it. It is read only by a human opening the migration.",
      "resolution": "OPEN and PERMANENTLY UNCORRECTABLE AT SOURCE — recorded so no future session quotes '146 DBF-IDs' as the current count. V22 is applied and Flyway-checksummed, so the header can never be edited; and unlike the two COMMENT ON statements that V29__fin_comment_corrections.sql corrected forward, a header comment has no forward-correction channel at all — SQL can rewrite a catalog comment, not a comment inside an earlier file. V29 therefore left it alone ON PURPOSE, correcting only the two statements that ARE in the live catalog; that omission was a decision, not an oversight, and must not be 'fixed' later by editing V22. The mutable record is this entry plus the V22 HEADER COUNT line of the ALIGN self-check. Anyone needing the authoritative figures should read the DB Alignment Manifest (147 DBF ids) and the XM id definitions (2 XM, both ACTIVE), never V22's header. === UPDATE 2026-09-12 (final governance-state pass) — STILL OPEN, and one CORRECTION to this entry's own closing sentence. The '146 DBF-IDs' half is unchanged: V22__fin_schema.sql:4 was re-opened and re-read today, verbatim as quoted above, and the current count is still 147. That half can never be corrected at source and this entry remains the mutable record for it. THE CORRECTION concerns the same header's '1 XM (SOFT-READ -> MDL)', which the DETAIL above calls 'likewise historical, since XM-FIN-002 ... was registered later'. That is no longer the situation: XM-FIN-002 was RETIRED on 2026-09-12 when the SEC user-directory read was deleted, so FIN declares exactly one XM again and the header's '1 XM' is ACCIDENTALLY CORRECT once more. Nothing was fixed — the world moved back — and no session may cite the XM half as evidence that the header is reliable, because the DBF half is still wrong. THE CLOSING SENTENCE ABOVE IS ALSO NOW WRONG AND IS SUPERSEDED HERE: it directs a reader to 'the XM id definitions (2 XM, both ACTIVE)'. Read them as 1 XM, XM-FIN-001, ACTIVE — XM-FIN-002 is struck. The DB Alignment Manifest figure it also cites, 147 DBF ids, was re-counted today and is correct.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "FinErrorCodes.FIN_403_SOD_VIOLATION and the FIN-403-SOD-VIOLATION keys in both i18n bundles — a live error code with no throw site on any API",
      "detail": "Measured on 2026-09-12 with three greps over src/main/java/ and src/main/resources/i18n/, every hit read. The constant is declared at src/main/java/com/erp/fin/exception/FinErrorCodes.java:78 — `public static final String FIN_403_SOD_VIOLATION = \"FIN-403-SOD-VIOLATION\";`. The message key is live in BOTH bundles: messages.properties:142 ('The close-approval permission is separate from the entry-creation permission') and messages_ar.properties:139. But `grep -rn 'FIN_403_SOD_VIOLATION' src/main/java/` returns exactly TWO hits in the whole tree — the declaration itself and a {@link} javadoc cross-reference at FinErrorCodes.java:392 — and NOT ONE `throw`. Its only former throw sites were FinSeparationOfDutiesService (deleted 2026-09-12) and FiscalPeriodDomain.assertCanHardClose (deleted with it). No caller can ever receive this code. The Error Catalog row WAS struck, at _SECTIONS.md:407 and its P3_1 mirror, following the precedent set by the struck FIN-503 row — so the catalog is honest. The divergence is that the constant and the two bundle entries were DELIBERATELY LEFT IN PLACE by the session that struck the row. WHY IT MATTERS RATHER THAN BEING COSMETIC: it makes FIN's three-way error-code count (40 constants / 40 en keys / 40 ar keys, re-measured today as an exact set match) include one code that is unreachable, so any future session diffing registered codes against reachable behaviour will re-discover the same thing; and a struck catalog row sitting beside a live constant and two live messages is exactly the shape that invites a later session to 'restore' the missing rule.",
      "resolution": "OPEN — DELIBERATELY NOT ACTIONED, and the one thing that must NOT be done is named first. NOBODY MAY RE-ADD A THROW FOR THIS CODE. RULE-FIN-015 (srs-fin.md:1026-1033) is satisfied in full by the @PreAuthorize(PERM_FIN_PERIODS_CLOSE_APPROVE) gate on FiscalPeriodService.hardClose and FiscalYearService.yearEndClose — a permission distinct from PERM_FIN_JOURNAL_ENTRIES_CREATE, which is exactly and only what the rule's Statement, REQ-FIN-038 and AC-FIN-038 require. Re-introducing a separation-of-duties check is precisely what src/main/java/com/erp/fin/domain/FiscalPeriodDomain.java:110-117 forbids in terms ('DO NOT RESTORE THE PREVIOUS IMPLEMENTATION'), and doing so would also re-create the retired XM-FIN-002 dependency on SEC. THE ACTUAL OPEN QUESTION IS NARROW and is for a human: whether to retire the constant and the two bundle rows alongside the struck catalog row, or to keep all three as the struck row's counterpart so the code is never silently reassigned to a different meaning. Both are defensible — removing them makes the 40/40/40 count describe only reachable codes; keeping them preserves the historical reservation of the string, the way the retired XM-FIN-002 id is preserved. This session invented neither answer and changed no source file. Carried as the SOD DEAD CODE line of the ALIGN self-check in _SECTIONS.md and its P3_1 mirror.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "SEC — RoleActionGrantRepository.findUserIdsHoldingPermission -> UserService -> SecUserDirectoryApiImpl: zero production callers after FIN's SoD removal (A.2.9 dead-code condition, OUTSIDE FIN)",
      "detail": "Traced on 2026-09-12 with `grep -rn findUserIdsHoldingPermission src/`, every hit read. The chain is three links plus its published interface: the JPQL query RoleActionGrantRepository.findUserIdsHoldingPermission (src/main/java/com/erp/sec/repository/RoleActionGrantRepository.java:124) is called ONLY by UserService.findUserIdsHoldingPermission (src/main/java/com/erp/sec/service/UserService.java:199-203), which is called ONLY by SecUserDirectoryApiImpl.findUserIdsHoldingPermission (src/main/java/com/erp/sec/crossmodule/SecUserDirectoryApiImpl.java:26-27), which implements SecUserDirectoryApi.findUserIdsHoldingPermission (:25). The only remaining callers of that interface method ANYWHERE are four assertions in a SEC integration test, src/test/java/com/erp/sec/SecCoverageIntegrationTest.java:438, :442, :447 and :452. There is no production call site left in any module. The cause is FIN's: FinSeparationOfDutiesService was the only production consumer, and it was deleted on 2026-09-12. This is an A.2.9 dead-code condition of exactly the kind FIN's own ALIGN-BE pass deleted four FIN repository methods for — with one difference worth stating: a test still exercises it, so the code is not unreachable, merely unused by production. A secondary staleness rides along: RoleActionGrantRepository.java:115's javadoc still names 'Caller: UserService.findUserIdsHoldingPermission (A.2.9)', which remains true one link up but is no longer true end-to-end.",
      "resolution": "OPEN, AND DELIBERATELY NOT ACTIONED BY FIN — reported here because FIN's change caused it, not because FIN may fix it. This is SEC's code and SEC's module record. FIN's ALIGN-BE block has no authority to delete SEC source, to shrink SEC's published crossmodule contract because its only consumer went away, or to edit SEC's execution-state. Nothing under src/main/java/com/erp/sec/ was touched by the session that recorded this. FOR A HUMAN, two defensible outcomes and no derivable one: (i) KEEP the method as published SEC API with no current consumer — legitimate, and there is a sibling precedent, since SecUserDirectoryApi.findContact is in the same position today (`grep -rn findContact src/main/java/` finds no production consumer outside SEC either), and a crossmodule interface exists to be consumed by modules that do not exist yet; or (ii) RETIRE the chain under A.2.9 the way FIN retired its own four dead repository methods, which would also mean retiring the four assertions in SecCoverageIntegrationTest that are now its only exercise. If (i) is chosen, RoleActionGrantRepository.java:115's javadoc should be amended to say the chain terminates at a published interface with no current consumer, so the next A.2.9 sweep does not re-open this. FIN has no stake in the answer and expresses no preference. Carried as the SEC DEAD CHAIN line of the ALIGN self-check in _SECTIONS.md and its P3_1 mirror.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "Error Catalog (_SECTIONS.md and its P3_1 mirror) — FIN-422-INVALID-PERCENTAGE-VALUE is a registered, thrown, fully-bundled code with NO catalog row (API-FIN-011 write, API-FIN-020 failure)",
      "detail": "Found on 2026-09-12 by a full-catalog sweep, measured rather than spotted: the 41 codes in the Error Catalog table's first column (_SECTIONS.md:378-418 inclusive) were extracted and `comm`-diffed against the 40 `\"FIN-*\"` string literals in FinErrorCodes.java. The catalog-minus-constants set is the expected two — FIN-500 (infrastructure fallthrough, :418) and the struck FIN-503 (:417). The constants-minus-catalog set is exactly ONE element: FIN-422-INVALID-PERCENTAGE-VALUE. It is not a phantom constant. The declaration is at src/main/java/com/erp/fin/exception/FinErrorCodes.java:357; it IS thrown, at src/main/java/com/erp/fin/domain/EventTypeRuleDomain.java:267, inside sourcedAmount(...) when `new BigDecimal(amountSourceValue)` raises NumberFormatException for a PERCENTAGE-sourced rule line; and both bundles carry the key, messages.properties:170 ('This rule line's amount source value is not a valid percentage number') and messages_ar.properties:167. `grep -n 'INVALID-PERCENTAGE'` over _SECTIONS.md returns ZERO and over P3_1/backend-execution-plan-fin.md returns ZERO, so the row is missing from BOTH mirrors identically — this is a plain omission, not a mirror divergence. The coverage half of the same gap was already found and closed by the test session: backend-test-plan-fin.md's header (lines 7-8) records TC-FIN-109 as ADDED 2026-09-12 expressly to close the FIN-422-INVALID-PERCENTAGE-VALUE gap, and the TC itself sits at :1543-1576. Only the catalog half remains. Note this is NOT the earlier 'Error Catalog rows FIN-404-YEAR and FIN-404-PERIOD' entry reopening: that one was about under-listed API columns on rows that exist, and it stays closed; this is a missing row, a different defect in the opposite direction.",
      "resolution": "OPEN — NOT FIXED BY THIS SESSION, which is scoped to the ALIGN self-check block, this file and one project-artifacts report; the Error Catalog table lies outside all three. THE FIX IS MECHANICAL AND MUST BE APPLIED TO BOTH MIRRORS BYTE-IDENTICALLY (_SECTIONS.md's table and the same table inside P3_1/backend-execution-plan-fin.md), by a session authorised to edit them: add one row with code FIN-422-INVALID-PERCENTAGE-VALUE; RULE column 'RULE-FIN-010 (build half)', matching the trace backend-test-plan-fin.md already assigns TC-FIN-109; API column 'API-FIN-020, API-FIN-011' — the failure surfaces at API-FIN-020 but the bad value is written at API-FIN-011, and both belong there for the same reason FIN-409-REMAINDER-COUNT lists its write and post endpoints together; HTTP 422, matching Status.BUSINESS_RULE_VIOLATION at the throw site; trigger 'a PERCENTAGE-sourced rule line whose amountSourceValue is not a well-formed decimal'; and the ar+en message columns COPIED FROM THE BUNDLES (messages_ar.properties:167 and messages.properties:170) rather than reworded, so the catalog and the wire cannot drift. UNTIL THAT ROW EXISTS, the ALIGN block's count relation must be read precisely: 41 catalog rows and 40 constants is a 40-row overlap plus two catalog-only entries (FIN-500, FIN-503) and one constant-only entry (this code) — NOT a superset in either direction. A related but separate finding — why a malformed value can be stored at all — is recorded as its own entry below. Carried as the CATALOG GAP line of the ALIGN self-check in _SECTIONS.md and its P3_1 mirror.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MAPPING_GAP",
      "phase": "ALIGN-BE",
      "sub": "ALIGN-BE",
      "endpoint": "POST /api/v1/fin/event-rules/{id}/lines (API-FIN-011) — RuleLineCreateRequest.amountSourceValue is an unconstrained String, so a malformed PERCENTAGE value is persisted and fails only later at API-FIN-020",
      "detail": "Read in source on 2026-09-12 at src/main/java/com/erp/fin/dto/RuleLineCreateRequest.java:41-42. The field is declared `private String amountSourceValue;` carrying a single `@Schema(description = \"Amount source value - قيمة مصدر المبلغ\", example = \"netAmount\")` and NOTHING ELSE — no @NotBlank, no @Size, no @Pattern, no format constraint of any kind. The contrast is local and deliberate-looking rather than accidental: the immediately preceding field amountSourceTypeCode carries @Size(max = 20) and the following one carries @NotBlank. TRACED END TO END: API-FIN-011 (create rule line) persists whatever arrives, including a value that can never be parsed as a decimal, on a line whose amountSourceTypeCode is PERCENTAGE. Nothing rejects it at write time and nothing warns. The failure surfaces only much later, at API-FIN-020 (post from event), when EventTypeRuleDomain.sourcedAmount(...) reaches `new BigDecimal(amountSourceValue)` at EventTypeRuleDomain.java:265-268 and converts the NumberFormatException into FIN-422-INVALID-PERCENTAGE-VALUE. THAT IS THE WHOLE REASON THAT ERROR CODE IS REACHABLE AT ALL — it exists to catch a malformed value the write path should never have accepted. The practical shape of the defect: a configuration mistake made on one screen by one user is reported as a posting failure on another screen, to a different user, possibly much later; and the stored rule stays broken, because FIN publishes create-only on rule lines — there is no update and no delete endpoint for a rule line (recorded separately under SCOPE vs SRS and srs-fin.md:1235), so the only remedy is to create a whole replacement rule.",
      "resolution": "OPEN — A REAL INPUT-VALIDATION GAP, not a documentation defect, and deliberately NOT fixed by this session, which edits state and self-check records only and writes no production code. TWO REASONS IT IS NOT A ONE-LINE FIX, both of which a human must weigh. FIRST, IT IS AN API CONTRACT CHANGE: adding a constraint to API-FIN-011 means a request body that is accepted today would start returning 400 VALIDATION_ERROR, and TestSprite and JUnit scenarios that post rule lines would need re-checking against the new rejection. SECOND, THE CORRECT CONSTRAINT IS CONDITIONAL AND NO RULE STATES IT: amountSourceValue is overloaded by amountSourceTypeCode — for a FIELD-sourced line it is an event-payload FIELD NAME (the @Schema example is literally 'netAmount', and EventTypeRuleDomain looks it up in the eventAmounts map), for a PERCENTAGE-sourced line it must be a decimal number, and for a REMAINDER-sourced line it is not read at all. A flat @Pattern is therefore wrong, and a class-level cross-field validator is a design decision that no REQ-FIN-*, AC-FIN-* or RULE-FIN-* specifies — RULE-FIN-010 governs how the remainder is computed, not how the source value is spelled. THE TWO DEFENSIBLE OUTCOMES: (i) add conditional validation at API-FIN-011 (a class-level constraint keyed on amountSourceTypeCode) and downgrade FIN-422-INVALID-PERCENTAGE-VALUE to a defence-in-depth path that should be unreachable in practice — the stronger product, since it fails at the screen where the mistake is made; or (ii) accept late failure deliberately, and say so in srs-fin.md so that a posting-time 422 for a configuration error is documented behaviour rather than a surprise. Either way FIN-422-INVALID-PERCENTAGE-VALUE still needs its missing Error Catalog row — see the CATALOG GAP entry above, which is a separate finding and does not depend on this one being settled. Carried as the INPUT VALIDATION line of the ALIGN self-check in _SECTIONS.md and its P3_1 mirror.",
      "recorded_at": "2026-09-12"
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "SVC-API",
      "sub": "SVC-API-INT",
      "endpoint": "ENT-FIN-010.amountSourceValue (AMOUNT_SOURCE_TYPE=FIELD) — API-FIN-020",
      "detail": "ESCALATED, NOT RESOLVED — specification silent, no rule invented. EventTypeRuleDomain.sourcedAmount (src/main/java/com/erp/fin/domain/EventTypeRuleDomain.java:274-276) returns BigDecimal.ZERO when a FIELD-sourced rule line names an amount the incoming event does not carry, instead of raising. The zero-amount line then violates the Postgres check constraint chk_fin_journal_line_amount_positive, so the caller receives 409/DATA_INTEGRITY_VIOLATION — a raw persistence failure, not a FIN-* code. This contradicts api-verify-config.md section 3 (error code format {MOD}-{http}[-{SLUG}]) and the gov-enforce-error-handling skill (every exception a LocalizedException carrying a registered module code). No artifact states an expected outcome for this input: srs-fin.md:228 only requires amountSourceValue to be present unless the type is REMAINDER, and says nothing about the event failing to carry the named field at run time. Found by the 2026-09-12 api-verify run and recorded as a stage-E observation, never asserted.",
      "resolution": "OPEN — needs a human decision between two options, neither of which may be chosen by an agent: (a) raise a NEW registered FIN error code for an unresolvable amount source, which is a new error code and therefore requires an explicit recorded human decision; or (b) declare the silent-zero degrade intended and write that down in srs-fin.md, the Error Catalog and the execution plan. Until decided, nothing in FIN changes."
    },
    {
      "type": "MISSING_IN_DOCS",
      "phase": "SVC-API",
      "sub": "SVC-API-SEARCH",
      "endpoint": "every POST .../search endpoint — platform SpecBuilder filter handling",
      "detail": "ESCALATED, NOT RESOLVED — specification silent, no rule invented. An unrecognized SORT field is rejected with FIN-400-INVALID-SORT (TC-FIN-064, TC-FIN-095, both pass), but an unrecognized FILTER field is silently ignored and the unfiltered page is returned. Observed on MDL's shared com.erp.common.search.SpecBuilder path, so it is platform-wide, not FIN-specific. This is not hypothetical: during the 2026-09-12 api-verify run a precondition filtered MDL lookup types on 'code' when the filterable field is 'key', the filter was dropped, the first unrelated row (NOTIF_CHANNEL) came back, and a lookup value was nearly seeded into the wrong lookup type. A silently-ignored filter is indistinguishable from a filter that matched everything.",
      "resolution": "OPEN — needs a human decision. Symmetry with the sort-field behaviour argues for rejecting an unknown filter field, but that is a cross-module contract change affecting every search endpoint in every module, and no artifact specifies it. Out of FIN's scope to decide unilaterally; raised here because a FIN run is what exposed it."
    },
    {
      "type": "MAPPING_GAP",
      "phase": "SVC-API",
      "sub": "SVC-API-CRUD",
      "endpoint": "TC-FIN-056 step (d) — API-FIN-019 POST /api/v1/fin/journal-entries with journalTypeCode=CLOSING",
      "detail": "ESCALATED, NOT RESOLVED — scenario line disagrees with delivered behaviour; neither was bent to match the other. TC-FIN-056's Expected line (packages/backend-test/RULE-SCENARIOS.md) states that a manual entry carrying journalTypeCode=\"CLOSING\" into a HARD_CLOSE period is 'still rejected 409 FIN-409-PERIOD-NOT-OPEN'. It IS rejected and nothing posts, but with 400 FIN-400-INVALID-LOOKUP: JournalEntryService.createManual calls lookupValidation.assertValidCode(KEY_JOURNAL_TYPE, ...) at line 179-180, BEFORE JournalEntryDomain.checkTargetPeriodOpen at line 194, and V26__fin_mdl_lookup_seed.sql:140-144 seeds only five JOURNAL_TYPE values (EVENT_GENERATED, MANUAL, RECURRING, ALLOCATION, REVERSAL) — CLOSING and OPENING exist only as Java constants in JournalEntryDomain and are never seeded, so the lookup gate fires first. This is a spec mismatch, NOT an application defect: the entry is refused and nothing is posted either way.",
      "resolution": "OPEN — needs a human decision between: (a) correct TC-FIN-056's Expected line to 400 FIN-400-INVALID-LOOKUP, or (b) seed CLOSING/OPENING as JOURNAL_TYPE lookup values, which would make the scenario's 409 reachable but would also let a caller name them. NOTE the underlying claim the scenario exists to prove is already PROVEN, and more strongly than the line as written: FinYearEndCoverageIntegrationTest asserts that the same entry sent as journalTypeCode=MANUAL into the same HARD_CLOSE period IS rejected 409 FIN-409-PERIOD-NOT-OPEN, and JournalEntryDomain.checkTargetPeriodOpen takes no journalTypeCode argument at all, so no caller-supplied journal type can ever reach the year-end carve-out."
    }
  ]
}
