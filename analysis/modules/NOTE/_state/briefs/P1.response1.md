P1 (SRS) for **NOTE v1** is written and analyzes clean.

**Files written**
- `erp/modules/NOTE/P1/srs-note.md` — PART A (A1–A8), PART B (one screen block B1–B5), STANDALONE (traceability, decisions applied, access summary)
- `erp/modules/NOTE/P1/registry-srs-note.md`
- `erp/decisions/NOTE/ADR-NOTE-001.md`, `ADR-NOTE-002.md`, `ADR-NOTE-003.md`

**Counts** — REQ 18 · AC 24 · ENT 1 · RULE 7 · SCR-REQ 1 · ADR 3. Every P0.5 story (US-NOTE-001…007) is covered; no question raised anywhere.

**Shape of the module** — one PRIVATE `master` entity `ENT-NOTE-001` (notePk, noteTitle, noteBody, ownerUserRef, isActiveFl + four audit fields), one composite screen `SCR-REQ-NOTE-001` on page code `NOTE_NOTES` with VIEW/CREATE/UPDATE/DELETE, no lookup key owned or consumed, no XM row and no FK (ADR-FIN-001 precedent), A7 not applicable (two states).

**Ambiguities self-resolved as ADRs, all non-breaking, pass continued**
1. `ADR-NOTE-001` — the entity stays `master` but carries no `nameAr`/`nameEn`/`code`: POL-NOTE-005 makes note content single-language and §3.3 NUMBERING fails all three business-number tests. This is a declared deviation against profile check `ERP-1`, resting on an approved policy rather than an engine judgement — worth the human eye at the gate.
2. `ADR-NOTE-002` — title ≤ 200 chars, body ≤ 4000: no input names a number, and an unbounded field leaves the only limit in the database, where the failure has no business message.
3. `ADR-NOTE-003` — a non-owner's request is answered "الملاحظة غير موجودة / The note was not found", not "forbidden", so the reply never confirms another user's note exists.

**Verification** — `gov.py analyze -m NOTE --scope stage:P1` → contracts C5, C12: **0 critical · 0 major · 0 minor · CLEAN** (ears 18, REQ→US 18, AC→REQ 24, RULE→REQ 7, registry-agree 50 ids, data-source clean). Write → registry → analyze is done; the commit and the gate are the orchestrator's to run.
