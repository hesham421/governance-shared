P3.2 is written and verified. I ran as the operator, so the files are on disk rather than emitted as `<<<FILE>>>` blocks.

## Written

| File | What it carries |
|---|---|
| `governance-shared/analysis/modules/MDL/P3_2/flow-diagram-mdl.md` | 4 flows, each citing a `US-*` and an SRS-backed `SCR-*`; `US-MDL-003` explicitly carries no flow (server-to-server read, no screen) |
| `…/P3_2/ui-ux-spec-mdl.md` | `SCR-MDL-001`, `SCR-MDL-002` with UI pattern, sub-views, full field tables (label per language, read-only flags), `Composition`, permissions, cross-module data, `States`; `UXD-MDL-001`; the B1–B4 reconciliation |
| `…/P3_2/frontend-execution-plan-mdl.md` | API SURFACE binding (11 REQ→API rows, codes→RULE), 6 phases, **8 SUBs** (one per screen in F1–F4), SEC-FE, ALIGN-FE + operations coverage, hand-off |
| `…/P3_2/registry-exec-fe-mdl.md` | ID ranges, screens, composition, UXD index, API coverage, shape diffs, ADRs, traceability |

IDs continue and are not renumbered: `SCR-MDL-001..002`, `UXD-MDL-001` — the same assignment the ADRs on disk already cite. Nothing new was minted.

## Three ADRs raised (all ACCEPTED, non-breaking — no question, no BLOCKED)

- **ADR-MDL-011** — the re-fetched api-docs now publish a `Contract ID` per endpoint (11 of 11), so the plan cites `API-MDL-*` again. Supersedes ADR-MDL-008, exactly as that ADR predicted its own end.
- **ADR-MDL-012** — no published surface tells a screen which *actions* its caller holds (SEC publishes an effective menu of page codes only), so VIEW is a real gate and CREATE/UPDATE/DELETE are answered by the server's 403; nothing is hidden on a guess.
- **ADR-MDL-013** — `UXD-MDL-001` reads a SEC endpoint gated by `PERM_SEC_MODULE_REGISTRY_VIEW`, so that grant must travel with `PERM_MDL_LOOKUPS_CREATE`; otherwise the owner-module select is empty and disabled.

## Checks

- `gov.py analyze --scope stage:P3.2` → **0 critical · 0 major · 0 minor, CLEAN**; `--scope pass:2` → CLEAN (the one MINOR is a pre-existing `feature-unwanted` against the SRS, a P1 artifact).
- `gov.py split --track frontend --module MDL --version 1 --dry-run` → 15 files, 0 findings, exit 0. (`verify FAILED (0 checked)` is the dry run's empty verification dict — nothing is written, so nothing is hashed; the report is `ok` under dry-run and the command does not block on it.)

Three things worth your eye:

1. I wrote a real `RESULT PASSED ✓ — 0 findings` line in ALIGN-FE rather than the "leave it alone" placeholder — a line C9.12 cannot read is a CRITICAL. It matches the report; the orchestrator can overwrite it.
2. Two statements from the previous revision are now deliberately different: the SRS in this version names **no** `activate` action, so those rows are gone from the operations coverage (only the two by-id reads remain `✗` under ADR-MDL-005), and the registry's traceability states **both** numbers — 13/13 REQ reached by an F-block, 11/13 traced by an `SCR`/`UXD` record — because the report's `req-ux` ratio is 84.6% and inflating it would assert a screen that does not exist.
3. Recorded, not corrected (both outside this stage's boundary): three reads the backend plan still predicts as `GET`, and `PERM_MDL_LOOKUPS_DELETE`, which the backend registry and the SRS declare but no published endpoint requires — both deactivate endpoints require UPDATE.