Stage P3.2 for MDL is written and verified. Four artifacts in `governance-shared/analysis/modules/MDL/P3_2/`:

- `flow-diagram-mdl.md` — 5 flows over `SCR-MDL-001..002`, plus the B1–B4 reconciliation
- `ui-ux-spec-mdl.md` — `UXD-MDL-001` + one block per screen, each with a `Composition` and a single-line `States`
- `frontend-execution-plan-mdl.md` — 6 phases in profile order, 8 phase-qualified SUBs (2 per `sub_bearing` phase), SEC-FE, ALIGN-FE, operations coverage
- `registry-exec-fe-mdl.md`

**The one substantive decision — ADR-MDL-008 (non-breaking, run continued).** The re-fetched `_inputs/api-docs-mdl.md` carries no governance `API-*` id: MDL's generator prints no `Contract ID` line, unlike SEC's and FIN's published docs. C9.5 [CRITICAL] requires every `API-*` the plan cites to be *defined in the fetched api-docs*, so citing `API-MDL-001..011` would have produced eleven CRITICAL findings and would have been a claim about a surface nobody published. The plan therefore cites no `API-*` id at all and binds each F-block by the identifiers the document does carry — verb, path and `operationId` — with a BINDING table at its head. The id ↔ endpoint mapping (1:1 across all eleven, matched on verb+path) is kept in the registry and in the ADR. The ALIGN API row reads `— examined nothing`, not ✓, and the COVERAGE row names C9.5.

The previous run's answer to this, ADR-MDL-001 (an annex inside the api-docs file), is marked SUPERSEDED: `fetch-inputs` regenerates that file, and the `keep_alongside` companion the factory now provides isn't read by `analyze`, which resolves the input to the merged file alone. The durable fix named in the ADR is in the backend repo — emit the `Contract ID` line MDL's generator omits, re-fetch, and C8.2, C8.3 and C9.5 all resolve with no plan change. ADR-MDL-002…007 carried over unchanged and still hold.

**Verification**

- `gov.py analyze -m MDL --scope stage:P3.2` → **0 critical · 0 major · 0 minor · CLEAN**; one vacuous clause, C9.5, exactly as the ADR predicts (C9.15/C9.16/C9.17 each examined 2 screens, C9.2 14 blocks).
- `gov.py split --track frontend --module MDL --version 1 --dry-run` → 15 files, 0 findings, exit 0.
- `--scope pass:2` went from 2 critical · 31 major to 1 critical · 29 major: this stage added no finding, and nothing in the remainder is attributed to a frontend artifact. What still blocks the gate is upstream and outside §8's boundary — C7.15 the backend plan's self-verdict, C7.11 `MDL-503`, C7.19 ten unwritten NOT NULL columns, C7.21 missing BOOTSTRAP DATA, C7.23, C8.3 ×11 and C8.4 ×3 (the same missing-id gap seen from P3.1's side), plus SRS minors.

I left the working tree uncommitted; `ADR-MDL-008.md` is new and `ADR-MDL-001.md` has the supersede note.