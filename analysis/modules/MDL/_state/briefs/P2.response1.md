Both artifacts are written and the stage check is clean. Here's what I did.

## Files written

| File | What |
|---|---|
| `governance-shared/analysis/modules/MDL/P2/db-script-mdl.md` | Header · DBF matrix (21) · XM register (1) · FULL_DATABASE_SCRIPT (blocks 1–11) · decisions + RULE→constraint map · DBF definition index |
| `governance-shared/analysis/modules/MDL/P2/registry-db-mdl.md` | Tables, DB sequences, 21 DBF ids enumerated, XM index, lookups (none), sequences, decisions, cascade, event |
| `governance-shared/analysis/decisions/MDL/ADR-MDL-009.md` | Index strategy (ambiguity §9, non-breaking) |
| `governance-shared/analysis/decisions/MDL/ADR-MDL-010.md` | `VARCHAR` precisions the SRS leaves unsized (§9, non-breaking) |

The ADRs are the brief's own ambiguity channel — §9 names "a precision" and "an index strategy" explicitly — so they sit beside the two mandated outputs rather than replacing anything. The ADR sequence continues at 009; 001–008 were raised at P3.2.

## Structure

2 tables, 21 DBF, 1 XM — the exact shape `project-registry` already pinned for MDL v1 (`MDL_LOOKUP_TYPE`, `MDL_LOOKUP_VALUE`, `DBF-MDL-001…021`, `XM-MDL-001 SOFT-READ → SEC`), so no name or id moved under the downstream plan. PKs follow `profile.stack.db.pk_generation = sequence`: one `SEQ_{TABLE}` each, plain `BIGINT NOT NULL`, no identity clause anywhere. RULE-MDL-002 is the one rule that becomes structure (`UQ_MDL_LOOKUP_VALUE_TYPE_CODE`); the other three are application-layer by the SRS's own wording, and BLOCK 6 says so instead of inventing a trigger. No seed block: MDL owns no lookup key (A6), so none is owed to another module's table either.

## Verification

`gov.py analyze -m MDL --scope stage:P2` → **0 critical · 0 major · 0 minor · CLEAN** (C6.2 21 subjects, C6.3 1, C6.5 22, C6.6 2 — the same coverage the pre-migration report recorded).

## Two things to flag

1. **The P3.1 plan's PK line is stale.** `backend-execution-plan-mdl.md` states `GENERATED ALWAYS AS IDENTITY` in both BINDINGS lines. The profile now says `sequence`, the brief forbids the identity clause outright, and the delivered migrations use `SEQ_<TABLE>` (recorded for the sibling module in `db-script-fin.md` §1, migration V22). I followed the profile and recorded the divergence in §4.2; P3.1 needs a re-run per engine §8 to correct those two lines. Nothing else in that plan is affected — it references columns by DBF id.

2. **The 11 `C7.19 required-writer` findings against `db-script` in the full-module run are pre-existing, not new.** They fire because the P3.1 plan names properties (`key`, `nameAr`) rather than DBF ids on its `Request:` lines. SEC's and FIN's untouched reports carry 57 and 73 of the identical finding. MDL's full run is BLOCKED on other stages' artifacts (missing frontend-test-plan, a stale verdict line, `MDL-503`, three endpoint-path disagreements); no finding names `registry-db`.

Completion is still yours: `gov.py state -m MDL` to refresh `_state/current-db-script.md` (MDL has no pointer — the artifact was absent from the repo entirely, with no git history), then the registry update and commit.