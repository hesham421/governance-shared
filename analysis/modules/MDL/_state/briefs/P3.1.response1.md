Both files are written and validated.

## What was produced

- `governance-shared/analysis/modules/MDL/P3_1/backend-execution-plan-mdl.md`
- `governance-shared/analysis/modules/MDL/P3_1/registry-exec-be-mdl.md`

**Validation** (run as operator, §11):
- `gov.py split --track backend --module MDL --version 1 --dry-run` → 12 files, **0 findings** (the "verify FAILED (0 checked)" tail is dry-run cosmetics — nothing is written to hash-verify, and it does not set the blocked return).
- `gov.py analyze --scope stage:P3.1` → **0 critical · 0 major · 0 minor · CLEAN**, `skipped: []`, every clause examining ≥1 subject.
- `gov.py analyze --scope gate:pass-1` → CLEAN; one MINOR against the **SRS** (`C5.16`, US-MDL-005 has no `unwanted` path) which is a P1 artifact outside this stage.
- `gov.py analyze --scope all` → the only other findings are the missing `frontend-test-plan` / `test-execution-manifest` (test-gen stage), pre-existing and unrelated.
- `C8.4 endpoint-agrees` examined all 11 endpoints with no finding — the plan's verbs and paths now match the published surface.

**IDs**: `API-MDL-001..011` and `QR-MDL-001..015` are the ranges this module version already carries (recovered from registry-db and the downstream frontend registry, so every existing citation still resolves). The only ids minted are `QR-MDL-016`/`QR-MDL-017`, the two sequence allocations.

**Four statements of the gated revision that described a system nobody built, corrected here**: `IDENTITY` PK generation → `sequence` + named sequences (what db-script §4.2 asked this stage for); three `GET` reads → `POST …/search` (ADR-MDL-002); the XM "REST call" → injected in-process interface (ERP-6); and the 503 catalog row struck — a network failure that cannot occur in one deployable, on a status the profile does not declare.

**No ADR was raised** and none was needed: every fork was settled by an input already in hand.

Two things I want to flag rather than bury:

1. **A silent check-hollowing bug I hit and fixed.** Three wrapped prose lines opened with an ID token (`XM-MDL-001 (…)`, `QR-MDL-002 (SAVE)`), which `idmodel.py` reads as *definitions* — the first one truncated the `ENT-MDL-001` record above its `OPERATIONS` line. Analyze still said CLEAN; only the report's `Examined` column showed it (`C7.20`: 9 subjects instead of 13). Reflowed; `C7.20` now reads 13. I saved this to memory since it will recur on every engine run.

2. **Something owed outside this module.** `XM-MDL-001` consumes a module-registry read on SEC's cross-module package, but SEC's own P3.1 artifacts register only `SecUserDirectoryApi` as an exposed surface. The dependency is real and live (`ENT-SEC-004` resolves, and `xref-resolve` passes), so I recorded it in INT-C and in the registry as owed to SEC's own re-run rather than asserting a surface SEC has not written down. I did not edit SEC's artifacts.